<!-- codex-review: model=gpt-5.5 -->

**Blocking**
- [supabase/migrations/0008_jobs_queue.sql:167](</Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/supabase/migrations/0008_jobs_queue.sql:167>) / [lib/cloud-sync/in-flight-job.ts:21](</Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/lib/cloud-sync/in-flight-job.ts:21>) - `dead_letter`/`cancelled` can still have a live worker that writes after the probe. Real interleaving:
  1. Cloud row `vidshared001`: `serialNumber=7`, `summaryMd=007_alpha.md`; local wants `003_alpha.md`.
  2. Dig job attempt 2 of 2 is `active`, lease expired, and the handler already pinned `base = 007_alpha` at [lib/job-queue/dig-handler.ts:51](</Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/lib/job-queue/dig-handler.ts:51>).
  3. Another worker calls `sweep_expired_leases`; because `attempts >= max_attempts`, SQL marks it `dead_letter` and clears lease columns at [supabase/migrations/0008_jobs_queue.sql:178](</Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/supabase/migrations/0008_jobs_queue.sql:178>).
  4. Sync probes `queued`/`active` only, sees no job, relocates `007_alpha -> 003_alpha`, and deletes old-base blobs at [lib/cloud-sync/reconcile-serial.ts:353](</Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/lib/cloud-sync/reconcile-serial.ts:353>).
  5. The original worker returns from Gemini before its next heartbeat notices lease loss. Its `ctx.signal.aborted` check at [lib/job-queue/dig-handler.ts:117](</Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/lib/job-queue/dig-handler.ts:117>) can still pass, then it writes paid output to `dig/007_alpha/120.r3.md` at [lib/job-queue/dig-handler.ts:119](</Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/lib/job-queue/dig-handler.ts:119>).
  
  Result: row points at `003_alpha.md`; newly paid dig is stranded under `dig/007_alpha/`. Same shape applies to `cancelled` when an expired active job had `cancel_requested=true`. `failed` is different: `fail_job` only sets `failed` after the handler has thrown, so that status does not later write. Retryable failures with attempts remaining become `queued`, so those are covered.

**High**
- [lib/cloud-sync/reconcile-serial.ts:239](</Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/lib/cloud-sync/reconcile-serial.ts:239>) - the residual enqueue-after-probe race is not milliseconds. Between the probe and metadata write there is `paidKeysUnder()` listing, plan construction, N blob copies, and a fresh metadata read before [lib/cloud-sync/reconcile-serial.ts:319](</Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/lib/cloud-sync/reconcile-serial.ts:319>). With many dig blobs this is multiple network round trips, easily seconds.
  
  Concrete interleaving: probe returns false for `vidshared001`; user triggers a dig immediately after; dig handler reads current row and pins `base=007_alpha`; sync copies old blobs, writes metadata to `003_alpha.md`, and cleanup deletes/listed old-base blobs. The dig then writes `dig/007_alpha/240.r3.md`. That paid output is unreachable because the row now derives `dig/003_alpha/`. This needs a DB-side fence/CAS or enqueue must be serialized against relocation; the current probe only narrows the pre-existing window.

**Low**
- [lib/cloud-sync/in-flight-job.ts:49](</Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/lib/cloud-sync/in-flight-job.ts:49>) - playlist resolution is by `playlist_key` only, while the schema only guarantees `unique(owner_id, playlist_key)` at [supabase/migrations/0001_core_schema.sql:17](</Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/supabase/migrations/0001_core_schema.sql:17>). The CLI passes a user JWT, and RLS scopes playlists to that owner at [supabase/migrations/0002_rls_policies.sql:4](</Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/supabase/migrations/0002_rls_policies.sql:4>), so normal use is okay. But the exported helper accepts any `SupabaseClient`; a service-role caller with duplicate playlist keys gets `maybeSingle()` failure and persistent `job-probe-unreadable`, or can cache a playlist id not tied to `deps.ownerId`. Pass `ownerId` into the probe and query `(owner_id, playlist_key)` to match the storage principal.

What I checked: the guard is before the relocation copy/delete/metadata phase in `reconcileCloudBase`; direct A3 relocation writes pass it. `queued` and `active` cover ordinary queued jobs, active jobs, retryable `fail_job` requeues, and expired active jobs before the reaper runs. `completed` is safe because `runOnce` calls the handler first and only completes after handler writes return. I did not find a production caller passing `noInFlightJobs`; it is used in local/fake tests, while the CLI and integration helper wire the real Supabase probe.
