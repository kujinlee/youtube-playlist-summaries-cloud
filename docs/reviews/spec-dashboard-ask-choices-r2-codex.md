<!-- codex-review: model=gpt-5.5 -->

**Severity** Blocking · **Where** §8a; parent `docs/superpowers/specs/2026-08-28-project-dashboard-design.md:117`; `scripts/check-dashboard-entry.py:241-254` · **REGRESSION INTRODUCED BY THE ROUND-1 FIX** · **What is wrong** §8a says `collect()` returns “added entry ids”, but entry ids are positional ordinals within a date, not stable identities. A diff can tell that a `## 2026-08-31` line was added, but not safely which current working-tree entry body that line denotes after same-date entries from base/other branches are present or after a branch edits/reorders entries. · **Why it matters** The gate can validate the wrong working-tree entry, or validate nothing useful, while the malformed added entry passes. This is exactly the high-risk caller §8a was meant to close. · **Suggested fix** Do not key the gate handoff by positional id; compare parsed base-vs-HEAD blocks by content/location, or parse both refs and validate entries present in HEAD but not base.

**Severity** Blocking · **Where** §8a; `scripts/gen-dashboard.py:294-315`; `scripts/check-dashboard-entry.py:24` · **REGRESSION INTRODUCED BY THE ROUND-1 FIX** · **What is wrong** §8a requires the gate to parse the store with “the shared parser”, but the only `parse_entries` lives in `gen-dashboard.py`, while the current architecture explicitly points `gen-dashboard -> check-dashboard-entry`, not the reverse. Importing `gen-dashboard.py` from the gate inverts the guard dependency and risks import/load side effects. · **Why it matters** The spec names a seam that is architecturally forbidden by the current code. An implementer either violates the gate boundary or writes a second parser, reintroducing drift. · **Suggested fix** Move `parse_entries` and decision parsing into an importable neutral module, or into `check-dashboard-entry.py` if the gate truly owns the grammar, then have the renderer import that.

**Severity** Blocking · **Where** §8a, §11 · **REGRESSION INTRODUCED BY THE ROUND-1 FIX** · **What is wrong** The cutover is based on the authored header date, but that date is user-controlled. A new post-cutover branch can add `## 2026-08-30 [needs-you]` with no `**Decide:**` block and bypass `decision_errors` forever because the header date is before the cutover. · **Why it matters** The new grammar gate fails open on the exact future malformed ask it exists to prevent. · **Suggested fix** Grandfather by “entry existed before cutover” using base-vs-HEAD membership, not by header date alone.

**Severity** High · **Where** §11; `.github/workflows/ci.yml:252-259` · **REGRESSION INTRODUCED BY THE ROUND-1 FIX** · **What is wrong** “The date this change merges” is not knowable when the PR check runs, and the spec does not say where the cutover literal lives or how it is updated if merge slips past midnight. · **Why it matters** A hardcoded intended merge date can silently become wrong, either breaking historical entries or exempting new ones. · **Suggested fix** Store an explicit reviewed cutover constant and define it as the first date whose newly added entries are validated, or avoid dates and grandfather the exact pre-cutover block set.

**Severity** High · **Where** §7 row 4, §8b; `scripts/gen-dashboard.py:439-451`, `scripts/gen-dashboard.py:691-716`, `scripts/gen-dashboard.py:759-763` · **What is wrong** v2 requires the tray to render “could not read one ask”, but the current data path still excludes errored entries from `unresolved()`, and `build()` builds the tray only from `unresolved(entries)` plus PR rows. Broken entries render later under “What changed”, not in the tray. §8b states the outcome but does not specify the mechanism. · **Why it matters** A malformed live ask can still disappear from the first, unfolded “What needs you” surface and fall through to “Nothing needs you.” · **Suggested fix** Define a separate tray input for malformed cutover `needs-you` entries, including how resolution is handled for them, before the empty-state branch.

**Severity** High · **Where** §6; `scripts/gen-dashboard.py:490-495` · **What is wrong** The new “total budget” for PR lookups is still not numeric and does not define whether the budget is elapsed time, call count, or both. It also renders budget exhaustion as generic “could not check”, indistinguishable from `gh` failure/bad JSON. · **Why it matters** Implementations can choose incompatible bounds, and a timeout-induced partial render will not tell the reader that checks were skipped because the budget was exhausted. · **Suggested fix** State a concrete total bound and render exhaustion as “could not check — PR lookup budget exhausted.”

**Severity** Medium · **Where** §6, §5a · **What is wrong** The population for PR lookup is ambiguous. §6 says “when an option carries `PR #N`, the renderer resolves N”, while §5a says decision blocks also render in entry cards with no special-casing. It is unclear whether PR resolution applies only to unresolved tray decisions or also to all decision blocks in all cards. · **Why it matters** Resolving all historical/card options makes lookup cost grow with the append-only store; resolving only tray rows means the same option renders differently between tray and card unless explicitly accepted. · **Suggested fix** Scope PR resolution to unresolved tray decision rows, and state that entry-card prose renders literal markdown only.

**Severity** Medium · **Where** §5a; `scripts/gen-dashboard.py:223-274`; `scripts/page_markup.py:153-220` · **What is wrong** “Rendered as ordinary markdown by the shared renderer” is not well-defined for decision lists. The current prose renderer splits blank-line paragraphs and runs inline markup; it does not render markdown lists as `<ul>/<li>`. · **Why it matters** The entry card’s decision block can render as a paragraph with raw `-` lines while the tray renders structured options, contradicting “must not silently render a different option list” unless that difference is explicitly allowed. · **Suggested fix** Either specify card prose remains paragraph-only/raw-list text, or extend the shared prose renderer to support lists and test the card/tray equivalence intentionally.

NOT CONVERGED
