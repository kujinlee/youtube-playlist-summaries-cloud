<!-- codex-review: model=gpt-5.4 -->

**Blocking**
None.

**High**
None.

**Medium**
None.

**Low**
None.

**Round-1 Fixes: Genuinely Fixed, Or Reworded?**
- `H1 — genuinely fixed.` The production serve path still passes the exact budget object at the only lease-holding call site: [VERIFIED: lib/html-doc/serve-doc.ts:127] `const model = await generateMagazineModelForServe(`, [VERIFIED: lib/html-doc/serve-doc.ts:130] `SERVE_BUDGET,                                   // REQUIRED — cannot be omitted`. The value assertion is not tautological because the test mock only shrinks RPC timeouts and imports the real budget object: [VERIFIED: tests/lib/html-doc/serve-doc-mapping.test.ts:20] `jest.mock('@/lib/serve-budget', () => ({`, [VERIFIED: tests/lib/html-doc/serve-doc-mapping.test.ts:22] `SERVE_RESERVE_RPC_TIMEOUT_MS: 20,`, [VERIFIED: tests/lib/html-doc/serve-doc-mapping.test.ts:42] `import { SERVE_BUDGET } from '@/lib/serve-budget';`. The import guard does discriminate the bounded names: [VERIFIED: tests/lib/html-doc/serve-bounded-import-guard.test.ts:24] `unbounded: /\\bgenerateMagazineModel\\b/,`, [VERIFIED: tests/lib/html-doc/serve-bounded-import-guard.test.ts:29] `unbounded: /\\bwriteModelEnvelope\\b/,`, [VERIFIED: tests/lib/html-doc/serve-bounded-import-guard.test.ts:49] `.filter(({ line }) => unbounded.test(line) && !line.trimStart().startsWith('//'));`. Mutation check: replacing the call-site budget with `{ attempts: 3, attemptTimeoutMs: 60_000, countTokensTimeoutMs: 10_000 }` made `tests/lib/html-doc/serve-doc-mapping.test.ts` fail on `expect(budgetArg).toEqual(SERVE_BUDGET)`.
- `H2 — genuinely fixed.` The timeout log is emitted in the same callback that rejects, so it cannot be outrun by the rejection: [VERIFIED: lib/html-doc/model-store.ts:85] `console.warn(`, [VERIFIED: lib/html-doc/model-store.ts:86] `` `[model-store] put exceeded ${timeoutMs}ms (elapsed ${Date.now() - startedAt}ms) for ` ``, [VERIFIED: lib/html-doc/model-store.ts:89] `reject(new DOMException(\`model put exceeded ${timeoutMs}ms\`, 'TimeoutError'));`. The test pins both key and elapsed-time content: [VERIFIED: tests/lib/html-doc/model-store.test.ts:135] `expect(logged).toContain('models/a-title.json');`, [VERIFIED: tests/lib/html-doc/model-store.test.ts:136] `expect(logged).toMatch(/elapsed \\d+ms/);`. Mutation check: removing the elapsed fragment made `tests/lib/html-doc/model-store.test.ts` fail on that regex.
- `H3 — genuinely fixed.` The code now distinguishes transport success from semantic success: [VERIFIED: lib/html-doc/serve-doc.ts:183] `const out = await callRpcBounded<boolean | null>(`, [VERIFIED: lib/html-doc/serve-doc.ts:189] `if (out.ok) {`, [VERIFIED: lib/html-doc/serve-doc.ts:195] `if (out.data === true) return true;`, [VERIFIED: lib/html-doc/serve-doc.ts:199] `console.warn(`, [VERIFIED: lib/html-doc/serve-doc.ts:203] `return false;`, [VERIFIED: lib/html-doc/serve-doc.ts:156] `if (releaseToken && !await settleBounded(supabaseClient, releaseToken, released) && released) {`, [VERIFIED: lib/html-doc/serve-doc.ts:157] `console.error('[serve-model] REFUND NOT APPLIED — the owner was charged for a failed generation');`. Empirical check on the fresh local Supabase stack after `npx supabase db reset --local --yes`: `reserve.data [{"status":"reserved","release_token":"..."}]`, then `settleOk.data raw true`, then `settleAgain.data raw false`. I did not observe `"true"`, row arrays, or `null` on the scalar-returning RPC.
- `M1 — genuinely fixed.` The retry count is now a named budget term and is used by the code that retries: [VERIFIED: lib/serve-budget.ts:65] `export const SERVE_SETTLE_ATTEMPTS = 2;`, [VERIFIED: lib/serve-budget.ts:80] `+ SERVE_SETTLE_ATTEMPTS * SERVE_SETTLE_RPC_TIMEOUT_MS;`, [VERIFIED: lib/html-doc/serve-doc.ts:178] `const attempts = released ? SERVE_SETTLE_ATTEMPTS : 1;`. The arithmetic test covers both terminal paths and the pathological combined case: [VERIFIED: tests/lib/serve-budget.test.ts:30] `it('BOTH terminal paths fit inside the enforced sum, independently', () => {`, [VERIFIED: tests/lib/serve-budget.test.ts:38] `expect(upToGeneration + SERVE_SETTLE_ATTEMPTS * SERVE_SETTLE_RPC_TIMEOUT_MS)`, [VERIFIED: tests/lib/serve-budget.test.ts:41] `expect(upToGeneration + SERVE_PUT_TIMEOUT_MS + SERVE_SETTLE_ATTEMPTS * SERVE_SETTLE_RPC_TIMEOUT_MS)`. `SERVE_FLOOR_SECONDS` is now `161`, still under the shipped `180`: [VERIFIED: lib/serve-budget.ts:90] `export const SERVE_FLOOR_SECONDS = Math.ceil(SERVE_FLOOR_MS / 1000);`, [VERIFIED: tests/lib/serve-budget.test.ts:59] `expect(SERVE_FLOOR_SECONDS).toBeLessThanOrEqual(SHIPPED_LEASE_TTL_SECONDS);`.
- `M2 — genuinely fixed.` All three migration literals are present and pinned: [VERIFIED: supabase/migrations/0024_lease_covers_serve.sql:54] `set lease_ttl_seconds = 161`, [VERIFIED: supabase/migrations/0024_lease_covers_serve.sql:55] `where lease_ttl_seconds < 161;`, [VERIFIED: supabase/migrations/0024_lease_covers_serve.sql:59] `add constraint guardrail_config_lease_ttl_covers_serve check (lease_ttl_seconds >= 161);`. The test asserts both population and values: [VERIFIED: tests/integration/serve-config-invariant.test.ts:183] `...[...sql.matchAll(/set\\s+lease_ttl_seconds\\s*=\\s*(\\d+)/g)].map((m) => ['set', m[1]] as const),`, [VERIFIED: tests/integration/serve-config-invariant.test.ts:184] `...[...sql.matchAll(/lease_ttl_seconds\\s*<\\s*(\\d+)/g)].map((m) => ['where', m[1]] as const),`, [VERIFIED: tests/integration/serve-config-invariant.test.ts:185] `...[...sql.matchAll(/lease_ttl_seconds\\s*>=\\s*(\\d+)\\s*\\)/g)].map((m) => ['check', m[1]] as const),`, [VERIFIED: tests/integration/serve-config-invariant.test.ts:189] `expect(literals.map(([where]) => where).sort()).toEqual(['check', 'set', 'where']);`. Mutation check: changing only the `where` literal to `160` made `serve-config-invariant` fail on the `where` entry.
- `L2 — genuinely fixed.` The migration now drops only lower-bound lease checks: [VERIFIED: supabase/migrations/0024_lease_covers_serve.sql:32] `and pg_get_constraintdef(oid) ~* 'lease_ttl_seconds[[:space:]]*>=';`, [VERIFIED: supabase/migrations/0024_lease_covers_serve.sql:39] `and pg_get_constraintdef(oid) ~* 'lease_ttl_seconds[[:space:]]*>='`. I verified Postgres’s normalized text, not just the source SQL: on a temporary table with the original `0012` constraint, `pg_get_constraintdef(oid)` returned `CHECK ((lease_ttl_seconds >= 1))`, and the regex matched. After a fresh `supabase db reset --local --yes`, the live catalog had exactly one lease lower-bound check on `guardrail_config`: `guardrail_config_lease_ttl_covers_serve | CHECK ((lease_ttl_seconds >= 161))`.

`CONVERGED`

Attacked and held:
- `npx tsc --noEmit` passed.
- `npm test -- --runInBand` passed: `263` suites, `2647` tests.
- Targeted live integrations passed after exporting the expected Supabase env aliases: `serve-config-invariant|reservation-release|serve-doc-materialize|serve-model-unreadable` = `4` suites, `52` tests.
- `npx supabase db reset --local --yes` reapplied `0001` through `0024` cleanly on Tuesday, August 11, 2026.
- Live RPC probe showed `settle_serve_model` returns raw boolean `true` on success and raw boolean `false` on a no-op repeat, which matches the fix’s `out.data === true` branch exactly.
- Mutation checks on H1, H2, and M2 all turned their intended tests red, so those guards are load-bearing rather than reworded.
