<!-- codex-review: model=gpt-5.5 -->

HIGH [MEASURED] Direct `service_role` DML can complete a pending generation without presenting the reservation token. Scenario: a future sync/import/bulk repair path uses table `UPDATE` instead of `record_artifact`, sets `state='complete'`, and bypasses the only fence at `docs/superpowers/specs/2026-08-03-stable-blob-addressing/schema/04_artifacts.sql:535-537` (`and g.state = 'pending'` / `and g.reserved_by = p_token`). Evidence: `docs/superpowers/specs/2026-08-03-stable-blob-addressing/schema/03_generations.sql:504` grants `select, insert, update, delete on video_generations to service_role`; measured probe result was:
`state=complete | reserved_by=11111111-1111-1111-1111-111111111111 | md_hash=SHA_DIRECT`.
Change: remove direct generation-completion authority from generic `service_role` table DML, or add a trigger/RPC-only transition that refuses `pending -> complete` unless the token-fenced protocol is used.

HIGH [REASONED] The caller contract test on PR #60 proves “abort signal delivered,” not “worker abandons rather than records,” so §12b’s load-bearing premise can pass while the runner still completes after lease loss. Scenario: heartbeat returns `ok:false`, handler observes `ctx.signal.abort` but returns paid bytes anyway; `runOnce` proceeds from `const result = await handler(job, ctx);` to `queue.complete(...)` at `lib/job-queue/worker-runner.ts:56-60`. The branch test’s handler does exactly that: it resolves on abort and then `return { done: true };` at `origin/chore/caller-contract-and-premise-tags:tests/lib/blob-addressing-caller-contract.test.ts:87-94`, and the assertion only checks `observedAbort` at line 100. Change: assert no terminal success write after lease loss, and/or have `runOnce` return `lost` if `signal.aborted` before `complete`.

MEDIUM [REASONED] The guard ratchet still reports “every guard classified” while excluding RLS policies, even though this schema treats policies as security guards. Scenario: a policy is removed or widened and `check-guard-coverage.py` can still pass because its catalog query only enumerates CHECKs, FKs, `_uq` unique indexes, and triggers at `scripts/check-guard-coverage.py:170-189`; meanwhile real policies exist at `schema/01_workspaces.sql:22`, `schema/03_generations.sql:78`, `schema/03_generations.sql:506`, and `schema/04_artifacts.sql:699`. Change: enumerate `pg_policy` for the schema-owned tables and classify those policies, or narrow the script’s success text so it does not claim total guard coverage.

MEDIUM [REASONED] The population ratchet’s “two callers” defect is documented but not fixed; it still counts repeated INSERTs, not distinct caller scenarios. Evidence: instrumentation stores only `(kind, paid, slot, op)` at `schema/05_assert.sql:101-105`, and the ratchet checks `w.op = 'INSERT' ... having count(*) > 1` at `schema/05_assert.sql:1801-1805`. Scenario: two fixture INSERTs from one setup block satisfy the ratchet while no loser/retry/second-caller path was exercised. Change: add a scenario/caller label to `t_writes` and require distinct labels per kind/paid/slot second-write coverage.

NOT CONVERGED
