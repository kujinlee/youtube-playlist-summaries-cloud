<!-- codex-review: model=gpt-5.5 -->

**Findings**

Medium, [scripts/check-plan-code.py:282](/Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/scripts/check-plan-code.py:282): the redirect is not a complete “cannot reach the reader’s live pages” guarantee. It only redirects `$HOME` / `USERPROFILE`. A child can still resolve the real account home via `pwd.getpwuid(os.getuid()).pw_dir` or named-user expansion like `Path("~kujinlee").expanduser()`. I measured this under fake `HOME`: `Path.home()` and `expanduser("~")` used the fake home, but `pwd.getpwuid(...)` and `expanduser("~kujinlee")` returned `/Users/kujinlee`. Concrete failure: a future manifested mutation or script path writes to `Path("~kujinlee").expanduser() / "explainers" / "dashboard.html"`; the three canary cases still pass because they only exercise `Path.home()`. Fix: narrow the claim to “protects current `Path.home()` / bare `~` defaults”, and add static refusal for `pwd.getpwuid`, named-user `~user` expansion, and hardcoded home literals in manifested targets. If the guarantee must be literal, run suites in filesystem isolation, not just an environment override.

Medium, [scripts/check-plan-code.py:620](/Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/scripts/check-plan-code.py:620): plan-assembly mode now runs suites with a redirected but nonexistent home, unlike `mutate_delivered`, which creates `.home` at [scripts/check-plan-code.py:454](/Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/scripts/check-plan-code.py:454). Concrete repro: a plan script whose `--self-test` does `(Path.home() / "direct-home-write.txt").write_text("x")` now fails with `FileNotFoundError` under `/tmp/.../.home/direct-home-write.txt`; under the developer’s real home it would have passed. That weakens the older plan-checking mode and contradicts the “costs zero fidelity” claim for all spawned suites. Fix: create `d / CHILD_HOME` inside `check()`’s temp tree too, while keeping `child_env()` side-effect-free.

Low, [scripts/check-plan-code.py:1059](/Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/scripts/check-plan-code.py:1059) and [scripts/check-plan-code.py:1080](/Users/kujinlee/code/agentic-ai-docs/youtube-playlist-summaries-cloud/scripts/check-plan-code.py:1080): the canary cleanup can delete a preexisting real-home file on basename collision. The temp basename is random, so this is unlikely, but the failure mode is real: if `~/explainers/harness-canary-<tempname>.html` already exists and the redirect is broken, the probe overwrites it with `"x"` and then `escaped.unlink()` removes it. Concurrent runs with different `TMPDIR`s can also theoretically choose the same basename and race on the same real-home canary. Fix: include a UUID/random token not derived only from the temp basename, preflight `escaped.exists()` before spawning, write a unique marker, and only unlink if the file content matches that marker.

**Measurements**

`python3 scripts/check-plan-code.py --self-test` passed: `139/139`.  
`python3 scripts/check-plan-code.py --mutate .` passed: `3 file(s), 73 mutation(s), 0 survivor(s)`.

I did not find a current manifested delivered-script spawn that bypasses `run_suite`; direct controls and per-mutation executions all flow through it. Nested subprocesses inherit the redirected environment unless the script supplies its own `env`.
