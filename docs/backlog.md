# Enhancement Backlog

Triaged feedback for enhancing the current version. Captured 2026-06-19.
Sequence/bundles below; `#8` is explicitly **someday** (vision, not near-term).

## Items

> **Severity markers — read this before scanning for red.** A leading 🔴/🟠/🟡/🟢 records severity
> **at filing**. It is *not* a live state. **On close, replace the marker with ✅ and keep the
> original in parentheses** — `✅ (was 🔴)`. Measured 2026-08-17: #36 and #37 both still led with
> 🔴/🟠 while their own Status cells said **FIXED**, so anyone scanning markers — a person or a
> future session — would have counted a critical blocker that did not exist. The Status column is
> the truth; the marker is a headline, and a stale headline is worse than none.
>
> ⚠ **The Status column can only be the truth if the row HAS one.** Five rows once carried two of the
> six columns, so the marker script read the *Item* cell as the status, found a ✅ written for
> something else, and closed **#46** and **#50** while both were open. Two stray blank lines had also
> split this table in three, and GFM ends a table at a blank line — so nineteen rows rendered as
> paragraphs of literal `|` on GitHub. Both are now enforced by `check_backlog_table_shape` in
> `scripts/check-docs.py`; **do not restate the rule here, and do not hand-count open rows** — derive
> them from the Status cell.

| # | Item | Touches | Size | Bundle | Status |
|---|------|---------|------|--------|--------|
| 31 | **Architectural / structural review gate in `dev-process.md`** — every review gate today is **per-task and per-diff** (Claude + Codex on the change), plus whole-branch rounds. Nothing ever asks *"is the overall structure still right?"* — module boundaries, duplication across `render.ts`/`render-deep-dive.ts` (cf. #5), layering between `lib/storage` adapters and routes, testability, whether the codebase is still navigable. `docs/dev-process.md` itself warns that "per-task review is structurally blind to composition defects", but the compensating step is only *more diff review*. **Proposal:** add a periodic (per-milestone, not per-task) **architecture review** step using `improve-codebase-architecture` (project skill, currently never invoked — see #18), informed by `CONTEXT.md` + `docs/adr/`. Decide in spec: cadence (per milestone vs per N merges), whether it is a gate or advisory, what artifact it produces (`docs/reviews/arch-<date>.md`?), and whether Codex runs an independent architectural pass. Note the skill is untried — evaluate it on the current codebase once before committing it to the process. | `docs/dev-process.md` (new phase/step) + first run output in `docs/reviews/` | M + design | (process) | *(was #17 until 2026-08-11 — `17` had been assigned TWICE; renumbered because the fencing item owns every inbound reference)* ✅ **ADOPTED 2026-07-30** — trial run done (`docs/reviews/architecture-review-2026-07-30.md`, 7 findings + 4 verified defects incl. a money-path one), Phase 6 added to `docs/dev-process.md` with the two lessons learned. Findings #1–#7 + D1–D4 are the follow-on work; #3 (in-memory adapter) in progress. |
| 18 | 🟡 **Skill inventory audit — trim the dormant, reinforce the load-bearing** — `scripts/skill-usage-audit.py` (added 2026-07-30) measured 460 sessions: **32 of 42 installed skills have never been invoked, typed, or injected**, and 4 skills (`brainstorming`, `writing-plans`, `subagent-driven-development`, `finishing-a-development-branch`) carry ~80% of all usage. 13 dormant project skills are the `mattpocock` set installed per `docs/plugins.md`, whose whole skill-conflict-resolution table is therefore largely unexercised. **Work:** (a) audit the dormant list and uninstall what is genuinely unwanted; (b) for dormant-but-wanted skills (`improve-codebase-architecture` → #31, `requesting-code-review`, `verification-before-completion`), find out *why* they never fire — missing trigger, superseded by a subagent path, or simply forgotten — and reinforce via `dev-process.md` or a hook; (c) prune the now-fictional routing rows from `docs/plugins.md`; (d) re-run the audit after trimming. **Blind spots to respect before deleting anything** (documented in the script's docstring): subagent-internal skill use is **invisible** (no `isSidechain` records exist in transcripts), so review/TDD skills may be undercounted; some dormant skills are internal helper contracts (`codex:codex-cli-runtime`); `superpowers:using-superpowers` shows **0 invocations but 57 hook injections** — invocation count alone would have wrongly marked it removable. | `scripts/skill-usage-audit.py` (done) + `docs/plugins.md` + `docs/available-skills.md` + settings | S–M | (process) | pending |
| 6 | 🟢 **Lead prominence** — gold "lead" line is over-emphasized (bold + larger + saturated gold) and competes with the section title. Make it non-bold (and/or italic), keep gold so it supplements rather than co-headlines. | `render.ts .lead` + `render-deep-dive.ts h2+p` (shared treatment) | XS | A | **in progress** |
| 7 | 🟢 **Drop bullet labels** — the bold `label:` prefix usually just repeats the sentence's first words and adds a competing black anchor per line. Drop it; experiment with inline emphasis instead (bold key term / italic / underline). | `render.ts` `<li>` + `generateMagazineModel` prompt | S | A | **in progress** |
| 5 | 🟢 **Palette dedup** — the shared magazine palette is copy-pasted across `render.ts` and `render-deep-dive.ts`. Extract a shared `BASE_PALETTE`. Opportunistic while both renderers are open for A. | `theme.ts` + both renderers | XS | A | **in progress** |
| 1 | 🟡 **Deep-dive version-aware regeneration** — deep-dive HTML serves a stale cache and the "Deep Dive" menu button silently always-regenerates with no hourglass. Bring it to summary parity: a `deepDiveVersion` (major = `.md`/prompt → re-generate; minor = CSS/render → cheap `runDeepDiveHtml` re-render), an `ensureDeepDiveHtml` orchestrator, a unified version-aware "Deep Dive doc" menu action, and the per-row hourglass. Regenerate trigger = version check for BOTH summary and deep-dive. | new orchestrator + route + `VideoMenu` + status bar + `deepDiveVersion` field | L | B | pending |
| 3 | 🟢 **Deep-dive timestamps** — clickable ▶ per-section YouTube timestamps in deep-dives. Now feasible because the deep-dive is transcript-grounded (PR #3). **Folds into Bundle B** (since #8 is someday). | deep-dive prompt tokens + resolver (mirror summary) | M | B | pending |
| 4 | 🟢 **Fence info-string guard** — `padDividers`/`parse.ts` treat a closing fence with an info string (` ```python `) as a valid closer. Pre-existing repo-wide limitation, low impact. Fold into any markdown-touching bundle or skip. | `markdown-dividers.ts` + `parse.ts` | XS | (loose) | pending |
| 2 | 🟡 **PDF story** — re-summarize leaves the PDF stale. Decide: keep PDF, or make the HTML printable and drop separate PDF generation ("print to PDF from HTML"). Needs a product decision before it's actionable. | pipeline PDF gen, print CSS, menu | M + decision | C | pending (needs decision) |
| 8 | 🟢 **Deep-dive as aligned expandable detail view** — reframe the deep-dive from an independent topic-organized doc into the section-aligned detail view of the summary: each summary section expands to a richer detail (text + diagrams + **screen captures** at the section's timestamp). Staged: (a) structural alignment (deep-dive sections keyed to summary sections + timestamps), (b) expand-in-place UI, (c) diagrams + screen-capture infra (needs downloadable video + ffmpeg; Gemini can describe frames but not extract them). **SOMEDAY** — vision, not near-term. | new pipeline arch + video-frame extraction + expand UI | XL / staged | D | **someday** |
| 13 | ✅ **Gated dev-login (local manual testing)** — the app login is **Google-OAuth only** (`app/login/page.tsx` → `signInWithOAuth({provider:'google'})`), and local Google OAuth needs client-id/secret env vars that are usually unset — so there is **no way to sign into the app UI against local Supabase (#2)** for manual testing. The app *has* `signInWithPassword` (used by the cloud-sync CLI); the UI just doesn't expose a form. Add a **dev-only email/password login** that renders **only** when pointed at a non-prod Supabase, **plus a guard test** asserting it is absent in prod. NOT a backdoor — a reviewed, environment-gated dev affordance. | `app/dev-login/page.tsx` server-gate + `DevLoginForm` + env gate + guard test | S | (dev/testing) | **✅ CLOSED — SHIPPED 2026-07-24** (`9a0806e`, PR #13). `app/dev-login/page.tsx` + `lib/supabase/dev-login.ts` (server-only `DEV_LOGIN_ENABLED`, `force-dynamic`) and the guard test `tests/lib/dev-login-flag-absent.test.ts` are all on `master`. The row was never ticked — it still said "IMPLEMENTED on `feat/dev-login-13`" after that branch merged. Found 2026-08-19 by the severity triage. |
| 15 | 🟡 **Eager magazine-model generation at ingestion** — today the rendered "magazine model" (the Gemini editorial pass: prose → lead + labeled bullets, `generateMagazineModel`) is generated **lazily** on first owner HTML serve, which is why HTML isn't ready for every video and why #14 exists. The model is **cheap** — ~1–1.5¢ actual (6¢ reserved worst-case, `magazine_est_cents`), ~1/6 of the ~6.5¢ MD/summary cost (the summary pays for audio transcription; the model only reads the MD text). Making it eagerly at ingestion (~15–20% ingestion-cost bump) would make HTML **always ready** for the whole list — no 503, instant share/view — and delete the lazy-serve / reservation-lease machinery for the common path. Key facts that de-risk it: **visual style is already decoupled** (`render.ts` is pure code, zero Gemini — CSS/theme changes re-render for free); only a **skim-*structure* prompt change** (`GENERATOR_VERSION`, now `magazine-skim v2`) forces model regen, and version-gating already regenerates lazily on next view, so eager baking doesn't strand anyone. **Even cheaper variant to weigh in the spec:** fold the lead/bullets into the *summary* generation call (near-zero marginal cost, one fewer call) — tradeoff is it re-couples skim structure to expensive summary regeneration. **Money/serve-path + core-pipeline touch → own design-spec → plan → SDD, not a quick edit.** Supersedes the need for #14's lazy-warm once shipped. | `worker`/ingestion pipeline + `generateMagazineModel` + `writeModelEnvelope` (+ maybe fold into summary prompt/schema) | M + design | (cloud) | pending (needs spec) |
| 14 | **Share-before-view returns "not ready" (503)** — a share link for a doc the owner has **never opened/downloaded as HTML** returns 503 until the owner materializes the rendered "magazine model". Found in prod testing 2026-07-28: share worked after an HTML download, failed before. This is **spec-correct** (the anon share path is a deliberately generate-free, never-charge leaf — `1f-b` D3: *"owner materializes by viewing once"*; spec line 168 already lists the intended follow-up: *heal-at-mint or an owner "refresh shares" action*), but it's a confusing UX gap: minting a share doesn't prepare what the share needs. **Fix:** when a share token is minted, best-effort warm the magazine model via the existing **owner** serve path (owner-authenticated → owner-charged is legitimate; a fresh model is a cheap no-charge blob read, and an absent/stale gen is bounded by `reserve_serve_model` per owner/doc/day — the same spend cap a normal owner *view* already incurs, no new/double charge). **Money/serve-path touch** → wants the standard light review gate, not a raw inline edit. Chosen: **synchronous client-side warm** (link revealed only after warm settles) via `ShareDialog` calling the existing owner serve path — no new server money-path code; the anon leaf stays generate-free. | `lib/client/api.ts` (`warmSummaryModel`) + `components/cloud/ShareDialog.tsx` | S | (cloud) | ✅ **MERGED to master (PR #37, merge `c49131d`, 2026-07-28)** — dual-reviewed CLEAN (0B/0H; money+security verified vs reserve/settle SQL), Low/Med findings addressed (15s warm timeout, telemetry, dismissal test), 2489 tests green. Reaches the live app on next deploy. |
| 16 | 🟡 **App-wide "new version available — refresh" banner** — after any deploy, a browser tab that was already open keeps running the **previously downloaded JavaScript** until a hard refresh, so users silently execute stale client code. Surfaced concretely by #14 (2026-07-29): the share pre-warm fix ran only after a `Cmd+Shift+R`; a tab open across the deploy minted a share with the old (no-warm) `ShareDialog` → dead 503 link, with **no signal to the user that a refresh was needed**. This affects **every** deploy, not just #14 — expecting users to know to refresh is not acceptable. **Fix:** detect a deployed-build-version change client-side (e.g. a stable `/api/version` returning the build id, polled/checked on focus or navigation; or Next.js `deploymentId` + skew handling — evaluate both) and show a **non-blocking** banner "A new version is available — Refresh" that the user clicks when convenient (never auto-reload — it would interrupt work / lose unsaved input). Note: **#15 removes the share-specific instance** of this (eager generation ⇒ no client warm to miss), but the general stale-tab problem needs this app-wide affordance. Decide in spec: version source (build-id endpoint vs `deploymentId`), check trigger (interval vs visibility/route-change), and banner placement/dismissal. | new `/api/version` (or `deploymentId`) + a small client version-checker hook + a banner component (Header/layout) | S–M + design | (cloud) | pending (needs spec) |
| 12 | 🟡 **Share dig-deeper (cloud)** — the anonymous share path serves the **summary only**: `app/s/[token]/route.ts` hardcodes `dig: false` and the share token carries no doc-type (`createShare(playlistId, videoId, ttl)`). Add the ability to share a dig-deeper view. Core constraint: extend the share serve route to render `dig: true` **only when the dig content is already materialized — never generate on the share path**, preserving the never-charges leaf invariant (money guard + `import-guard.test.ts`). Decide: (a) UX — a separate "Share with dig-deeper" option vs one link that shows dig when present; (b) access scoping / whether an existing summary-share upgrades; (c) the `🖼️` image-placeholder story (overlaps **M2b** image backfill — a shared dig view shows text placeholders until images land). **Needs its own design-spec → plan → SDD (cloud slice), not a config flip.** | `app/s/[token]/route.ts`, `ShareDialog.tsx`, `createShare`/`create_share_token` RPC, share serve ctx, renderer `dig` flag | M + design | (cloud) | pending (needs spec) |
| 17 | 🟠 **Fence the worker persist against sync (paid-blob orphaning)** — `runSync` has **no mutual exclusion against the worker**, and on the A3 relocation path that costs paid content rather than a wrong pointer. Concrete, confirmed interleaving (round 5 of `fix/serial-coherence-sync`, 2026-08-03): `summary-handler.ts:95-96` pins `baseName` from the serial it reserved, then spends **minutes** in transcription + Gemini before persisting at `:156`. If A3 relocates the base inside that window — cloud `serialNumber 7 / 007_alpha.md` → `3 / 003_alpha.md`, paid digs copied to `dig/003_alpha/` and `dig/007_alpha/*` deleted — the stale persist then lands: `persist_summary` resolves the key as `coalesce(p_video->>'summaryMd', v.data->>'summaryMd')` (`0021:135`, **payload wins**) while `serialNumber` is restored from the row by step (2). Final row = `serialNumber 3` beside `summaryMd 007_alpha.md`, with the paid digs stranded at `dig/003_alpha/`. **Widening A3's pre-write freshness check does NOT fix this** (Codex proposed it; rejected with reasons) — the worker has written nothing when A3 checks; its write lands strictly after. Only fencing closes it. **Decide in the spec:** lease/CAS on `persist_summary` (fence on the serial the worker reserved) vs serializing sync against the worker through the job queue. Must cover the **whole sync write path** — `transferClassA` (`sync-run.ts:427`) and `copyAdditiveVideo` (`:281`) carry the same exposure, so this is not an A3 fix. **Money/data-loss path → own design-spec → plan → SDD.** **PARTIAL MITIGATION SHIPPED 2026-08-04** (`fix/sync-skip-inflight-job`): A3 now refuses to relocate while a job for that video is pending, which closes the *wide* window (the minutes a job spends in Gemini). It does NOT close the residual one — the copy phase between the probe and the metadata write is N sequential blob round-trips, so a job enqueued and claimed inside that span still reads the pre-relocation serial. The durable fix below is still required. | `lib/job-queue/summary-handler.ts`, `supabase/migrations/*persist_summary*`, `lib/cloud-sync/sync-run.ts`, `lib/cloud-sync/reconcile-serial.ts` | M + design | (cloud) | **SPEC WRITTEN, NOT CONVERGED — deferred pending ADR-0006 (2026-08-05).** `docs/superpowers/specs/2026-08-04-cas-fence-persist-summary-design.md` (v5) + 12 review docs (`docs/reviews/spec-conditional-write-*`). **Five dual adversarial rounds produced 26 Blocking findings and NONE was in the predicate** — every one was in the protocol around it (NULL semantics, payload rebuild, publish semantics, status inheritance, deploy mechanics, address adoption, field-omission semantics). The conditional write itself was correct from the first draft. Work stopped deliberately, not from review fatigue: the surviving defects live in `persist_summary`'s merge semantics and the blob publish protocol, **both of which ADR-0006 replaces** — under the manifest, blobs never collide, publication is a conditional update on one row, and the `summaryMd`-vs-artifact-key split disappears. Split out along the way: **#19** (content race), **#20** (title-change orphaning, concurrent half closed by the spec's second conjunct), **#21** (dig writes), **#22** (promoted inheritance). **Revisit trigger:** the manifest slice — it either dissolves this or it does not, and by then §14 question 8 (do the Class-A scalars become generation-scoped?) will be answered. ⟳ **2026-08-07 — TRIGGERED, and the answer is BOTH. NARROWED, not closed.** The **address race — this item's original shape — IS dissolved** by ADR-0006: the key is `<ws>/videos/<vid>/<gen>/summary.md`, carrying neither serial nor slug, so there is no relocation for a stale write to race with. Most of the five-round CAS spec is **moot rather than deferred**. The **reservation race is not**, and round 5 of the manifest review made it worse — MEASURED: a lease expiring under a live worker, a second writer reclaiming, two paid Gemini calls in one slot. That half was never about addressing and is closed separately by the reservation protocol (PR #54, spec §9.2). **Residue still open:** whatever depends on `persist_summary`'s merge semantics, which ADR-0006 replaces but which has not yet been cut over. Re-read the CAS spec against the manifest before assuming any specific finding survived — the two halves failed for different reasons. |
| 19 | 🟠 **`transferClassA` content race — a worker persist can clobber a just-transferred winner body** — split out of #17 on 2026-08-04 when the CAS-fence spec was scoped, because a serial fence provably cannot catch it. `transferClassA` (`sync-run.ts:371-435`) overwrites the loser's MD blob at `key` and patches `summaryMd`/`artifacts` with the **winner's** content, but **never writes `serialNumber`** (verified: the `completeTuple` at `:398-431` has no such field). So a worker that reserved a slot before the transfer and persists after it will pass any `serialNumber`-keyed fence — the serial genuinely did not move — and layer (3) of `persist_summary` (`0021:133`, payload-wins) then replaces the transferred winner body's pointer with its own freshly generated one. **Severity is deliberately lower than #17 and that is why it was split**: this loses a *sync decision*, not paid blobs. Both sides still hold readable MD, so the next sync run re-compares and re-converges; nothing is orphaned and no Gemini spend is needed to recover. Contrast #17, which strands paid dig content that costs real money to regenerate. **Decide in its own spec:** whether the fix is to extend the fence to a content identity (`mdHash`/`mdGeneratedAt` — note the worker's own generation is legitimately newer, so "newest wins" may already be the correct outcome and this may be a non-defect), or to make `transferClassA` participate in the same in-flight-job probe A3 uses (`reconcile-serial.ts:240-251`). **Establish first whether it is a defect at all** — that is the cheapest possible outcome and it has not been ruled out. | `lib/cloud-sync/sync-run.ts` (`transferClassA`), `supabase/migrations/*persist_summary*`, `lib/job-queue/summary-handler.ts` | S–M + design | (cloud) | pending (needs spec) — split from #17, 2026-08-04 |
| 20 | 🟠 **A title change orphans every dig blob — the slug half of the base is unguarded** — found 2026-08-04 during the `grill-with-docs` pass on the conditional-write spec, by asking what the phrase "the address moved" actually covers. A video's blob address is `base = pad(serialNumber) + '_' + slugify(title)`. **Nothing validates the slug half.** The worker derives it fresh from the job payload each run (`summary-handler.ts:96`, `slugify(payload.title)`); A3's `applySerial` (`lib/serial-filename.ts:20-25`) *preserves* whatever slug it finds and only re-prefixes the serial; and `checkSerialInvariant` (`lib/serial-invariant.ts:54`) only appears to check it — it builds its expectation with `applySerial(value, serial)`, copying the slug out of the value under test, so the slug comparison **cannot fail**. (A guard that passes in both worlds — the fourth instance on this project.) **Consequence, reachable with NO concurrency:** re-summarize a video whose YouTube title changed → same serial, new slug, so the row's `summaryMd` moves to `<serial>_<new-slug>.md` while every dig section blob stays at `dig/<serial>_<old-slug>/`. Dig blobs are addressed by *derived* base with no stored pointer (`lib/dig/cloud/dig-blob-key.ts:22`, read back at `lib/dig/cloud/load-dig-for-serve.ts:32`), so they are orphaned — **the same paid content and the same damage as #17, by a completely different route**. Note `digDeeperMd` is NOT affected: it is a stored pointer preserved by `persist_summary` layer (2), so it keeps resolving; the loss is confined to the per-section dig blobs. **Deliberately NOT folded into #17:** that is a race needing a second writer and a window, this needs neither, so the mechanisms differ. **Decide in spec:** pin the slug at first ingest and re-prefix only (making base slug-stable by construction, matching what `applySerial` already assumes), vs. detect the change and migrate `dig/<old>/ → dig/<new>/` the way A3 does, vs. let ADR-0006's manifest dissolve it (a stored `blob_key` has no derived slug to drift) — **check the third option first; this may be work the stable-blob-addressing slice deletes rather than work to do.** Independently corroborates §4.2.1 of the stable-blob-addressing spec: the title is load-bearing identity, not decoration. | `lib/serial-invariant.ts`, `lib/serial-filename.ts`, `lib/job-queue/summary-handler.ts`, `lib/dig/cloud/dig-blob-key.ts` | M + design | (cloud) | **SCOPE NARROWED 2026-08-04** — the *concurrent* half is closed by the conditional-write spec (#17), which added a null-safe `summaryMd` conjunct after review round 1 proved `describeDivergence` relocates on slug-only divergence (`reconcile-serial.ts:150-155`, comment `:181-182`). What remains here is the **non-concurrent** half only: a re-summarize after a title change with no sync involved, where there is no second writer to compare against. Still pending, still needs a spec, but smaller than filed |
| 21 | 🟠 **Dig writes have the same stale-base exposure and a *different* sink** — split out of #17 on 2026-08-04 when both round-1 adversarial reviewers (Codex High, Claude Medium) independently found it. `dig-handler.ts:55-57` derives `base` from `resolveSummaryMdKey(video)`, spends minutes of billable Gemini at `:100-113`, then writes `dig/<base>/<sectionId>.rV.md` at `:119-125` via `write-dig-section-blob.ts`. If A3 relocates inside that window, the paid dig lands in a prefix A3 has **already deleted** — orphaned on arrival. **Why #17's fix does not cover it:** the conditional write guards `persist_summary`, and the dig handler never calls it. §0 of the #17 spec frames the design as "guard the sink, not the sources" — for digs the sink is a **blob write with no row to attach a condition to**, so the one-sink argument does not extend. The codebase already says this out loud and #17's spec had missed it: `in-flight-job.ts:69-70` — *"NOT FILTERED BY `job_kind`. Summary jobs and dig jobs BOTH pin `base` … A `job_kind` filter here would silently reopen the window for digs"* — and `reconcile-serial.ts:44-52` names `dig-handler.ts:51-57` as an equal hazard. So PR #45's probe **does** cover digs; only the durable guard does not. **Decide in spec:** re-check the base between generation and the blob write (cheap, narrows but does not close), vs. route the dig write through a guarded RPC that can fail the write atomically, vs. let ADR-0006's manifest dissolve it (a dig would be published by a conditional manifest update like any other slot — **check this first**, it may be work the stable-blob-addressing slice deletes). Note the damage here is *more* direct than #17's: #17 strands digs as collateral of a summary write, whereas this writes a paid dig straight into a deleted prefix. | `lib/job-queue/dig-handler.ts`, `lib/dig/cloud/write-dig-section-blob.ts`, `lib/cloud-sync/reconcile-serial.ts` | M + design | (cloud) | pending (needs spec) — split from #17, 2026-08-04 |
| 22 | 🟡 **Promoted-inheritance on a re-address — needs an artifact identity the row does not carry** — scoped out of #17 by decision on 2026-08-05, after **three** adversarial review rounds failed to fix it inside that slice. The key-scoped monotonic rule at `0021_cloud_sync_signals.sql:142-149` preserves `'promoted'` against a `'committed'` write **when the artifact key is unchanged**, on the assumption its own comment states at `:138-141`: *"A different key is a genuinely new artifact."* A re-address (#17 §6) makes the key the **same** while the artifact is **new**, falsifying it. **Two fixes were tried and both failed review, which is why this is filed rather than retried:** (a) *invert the ordering* — publish before writing metadata — replaced a **visible** 503 window with a **silent** one (`serve-summary-core.ts:50` returns 503 only for `committed`), and `SupabaseBlobStore.promote` being create-if-absent would have discarded the freshly generated bytes outright; (b) *a `p_artifact_is_new` caller flag* — **circular**: a worker stale in **content** whose **address never moved** passes the address guard cleanly and can then switch off the one defence aimed at it (measured: the heartbeat fires every `leaseSeconds/3` = **40 s**, `worker-runner.ts:47-52`, so a worker can run that long past lease loss with `ctx.signal.aborted` still false). The flag was also **absent where equally needed** — a same-key re-summarize at a new `docVersion` is just as much a new artifact. **Root cause, and why the manifest is the fix:** the row cannot express *"same key, different artifact"* because it carries no artifact identity. ADR-0006's manifest introduces exactly that (`generation_id`), and under it blobs never collide at all — so **check whether this is dissolved rather than fixed before writing any spec.** **EXECUTABLE EVIDENCE (added 2026-08-05):** two `it.failing` tripwires prove this defect is live and will go **RED the moment it is fixed** — `tests/lib/job-queue/summary-handler-promote-divergence.test.ts` (W1: a re-summarize after a doc-version bump keeps the OLD body while `persistSummary` stamps the NEW `docVersion`) and `tests/lib/dig/write-dig-section-blob-promote.test.ts` (W2: a re-dig at a bumped generator version keeps the stale body, with no metadata that would reveal it). Both were written and CONFIRMED on 2026-07-30, sat unmerged on a branch, and the conditional-write review then rediscovered the same defect from scratch **twice** (Codex round 2 B1, and B-R4-1 in round 4) — which is why they are landed as tripwires rather than left as prose. Mutation-verified: flip `promoteSemantics` to `'overwrite'` (i.e. simulate the fix) and the tripwire fires. **When the manifest slice dissolves this, these go red — that is the signal to close #22.** **Residual #17 ships with — CORRECTED 2026-08-05 after review round 4 measured the first version false.** On a re-address the intermediate `committed` write inherits the prior `promoted` for one publish, so a reader in that window gets the older copy instead of a 503. That was originally called bounded and non-destructive; **it was neither.** The same write also carried `docVersion` and the new run's scalars, satisfying **both** conjuncts of the idempotency skip (`summary-handler.ts:86-92`), so a fault inside the window froze **permanently** — retry skips, job reports success, new scalars sit over old bytes, paid output unrecoverable — while the *pre-slice* behaviour on that path **self-heals** (keys differ, `promoted` not preserved, 503, retry repairs). The residual was strictly LARGER than what it replaced. **The fix (#17 §6): withhold `docVersion` from the `committed` write, sending it only on the `promoted` one.** That breaks the skip's second conjunct, so a fault inside the window no longer freezes and the retry repairs. Only with that change is the residual genuinely a staleness window rather than corruption, and #17 §8 mutation-tests it instead of asserting it. | `supabase/migrations/*persist_summary*`, `lib/job-queue/summary-handler.ts`, `docs/adr/0006-stable-blob-addressing.md` | M + design | (cloud) | pending (needs spec) — scoped out of #17, 2026-08-05; likely dissolved by ADR-0006 |
| 23 | 🟠 **Corrections should be deterministic `{from, to}` pairs, not a free-form Gemini rewrite** — raised by the user on 2026-08-05 while reviewing the card decision in the stable-blob-addressing spec (§5.2.2). **Two defects and one design change.** *(a) A fresh summarize silently drops the user's corrections AND claims it did not.* `pipeline.ts:272` stamps `mdCorrectionsHash: mdHash('')`, and the cloud worker (`summary-handler.ts`) never mentions corrections at all — so `persist_summary`'s layer-2 merge **preserves the previous hash**. After a cloud re-summarize the body has no corrections applied while the row asserts `mdCorrectionsHash = <hash of the user's corrections>`. Same card/body lie as B-R4-1, except the lost content is something the **user typed**. Only `/regenerate` applies corrections (`route.ts:63`), and that path edits an existing document rather than producing a generation. *(b) Carry-forward is unaffordable by construction.* `fixSummary` (`gemini.ts:456`) hands free-form English to Gemini and gets **the whole document** back, so re-applying costs a full-document round trip per generation — which is why the code does not do it. Output tokens are the pricier side of the API and there are thousands of them to change two words; a cheaper model tier does not fix this (`SUMMARY_MODEL` is already `gemini-2.5-flash`). **Change:** corrections become a list of `{from, to}` pairs with word-boundary, case-preserving matching, applied deterministically at publish to the body **and** the card prose. An LLM still authors the pairs — that is what it is good at, and it is where the user's "handle all the variations" requirement is met (`Clawcode's`, `clawcode`, `Clawcodes`) — but it **never edits the document**. Output is ~50 tokens instead of thousands, so the authoring call is genuinely cheap; passing the document as input is optional and buys only variants present in *this* text. **Why not-LLM-applied is the point, beyond cost:** the model currently rewrites the whole paid document under an *instruction* ("do NOT restructure any sections") rather than a guarantee, and **section headings are an identity anchor** in this codebase (spec §4.2.1) — a reworded heading orphans paid digs. A weaker/cheaper model is *worse* at verbatim-except-two-words copying, so the cost fix and the safety fix point the same way only in this design. Deterministic pairs are also **assertable** (no occurrence of `from` survives; the diff touches only those spans) and **inspectable** — the user can review the pair list before it runs, which is impossible today. Bonus: applying the same pairs to `tldr`/`takeaways` removes the second Gemini call (`extractQuickView`) from the regenerate path. **Blocks:** stable-blob-addressing §5.2.2's rule that a generation is not publishable until corrections are applied — that rule is correct either way but ships as an expensive per-generation Gemini call unless this lands first. ⟳ **2026-08-06 — NARROWED.** The spec was recorded as blocked by this item; on inspection that was two claims and only §5.2.2 holds. The *hash representation* (round-6 B4) shipped independently in PR #53 by making "no corrections" a **defined** constant rather than one derived from the corrections shape, so an empty `{from,to}` list will hash to the same value without a redefinition. What #23 still owns for the spec: §5.2.2 itself, and re-authoring the **99 existing free-form corrections** into pairs — their `corrections_hash` is backfilled from the English text today and will be recomputed once, which is a migration re-run rather than a second decision. | `corrections` field shape + `gemini.ts:fixSummary` + `app/api/videos/[id]/regenerate` + `summary-handler.ts` + `pipeline.ts` + `CorrectionsPanel.tsx` + migration for the pair list | M + design | (cloud/quality) | pending — needs its own spec |
| 24 | 🟡 **Cloud slide placeholders become timestamp deep links (and an on-demand player)** — came out of the 2026-08-06 ToS review (ADR-0005 amendment). Cloud-side, a slide token currently renders as a caption-only blockquote with **no link at all** — `slideTokensToCaptions` in `lib/dig/cloud/parse-dig-section-blob.ts:11-17` emits `> 🖼 *caption*`. The caption was generated and **paid for**; the moment it describes is known; and the codebase already renders exactly this affordance elsewhere — `render-dig-deeper.ts:289` emits `▶ (mm:ss)` linking to `watch?v=<id>&t=<sec>s` for section headings. **Work:** give the slide placeholder the same treatment, then optionally expand to an inline `youtube-nocookie.com/embed/<id>?start=<sec>` player on click. **Deliberately NOT an always-on embed:** with `autoplay=0` a YouTube embed shows the video's *poster thumbnail*, not the frame at *T* (the frame appears only after playback, and mobile requires a user gesture), so an always-embedded player would show the **wrong** image while costing one full player per slide — and a dig-deeper section can carry several. Caption + link by default, embed on demand. **Verify the poster behaviour in a browser first; the design depends on it.** ToS-clean by construction: zero pixels traverse our servers and rendering happens in YouTube's own player (ADR-0005 amendment 2026-08-06). Closes the gap where a cloud reader is told *that* a visual exists and given no way to reach it. | `parse-dig-section-blob.ts` + the dig-deeper renderer + a small client component | S | (cloud/UX) | pending |
| 25 | 🟡 **Render addressing — renders need an identity, and two designs have already been refuted** — split out of ADR-0007 by user decision 2026-08-09. **Full brief with all evidence: [`docs/superpowers/specs/2026-08-09-render-addressing-brief.md`](superpowers/specs/2026-08-09-render-addressing-brief.md).** A render (HTML page / PDF) is free and reproducible, and is marked today by `generation_id IS NULL` — a single NULL meaning BOTH *'this is free'* (money) and *'this address may be overwritten'* (addressing). Five findings across rounds 8-12 came out of that conflation. **RULED OUT, do not re-propose:** (a) an identity enumerating version constants — there are THREE (`GENERATOR_VERSION`, `PDF_RENDER_VERSION`, `DIG_GENERATOR_VERSION`) plus a pinned Chromium, and `pdf-render-version.ts:5-9` says a missed bump **cannot be detected**; (b) an identity living only in a column while the key is unchanged — nothing binds `render_id` to `blob_key`, so N rows share one key and N-1 point at overwritten bytes (**strictly worse than today**, where `video_artifacts_free_uq` keeps row↔blob 1:1); (c) a `<gen>/`-shaped render key — it erases §8's segment-4 paid/free discriminator, making renders uncollectable or unsafe to collect. **HARD CONSTRAINT both attempts missed:** the PDF route computes the cache key from the HTML and probes the blob store *before* rendering (`app/api/pdf/[id]/route.ts:54,:60`), so an output-bytes hash **cannot be the probe key** — any design needs TWO keys, an input-derived probe and an output-derived identity. **Also surprising:** `renders/` appears in ZERO TypeScript files; it exists only in the spec table and fixtures, so §4.0's classifier classifies nothing live today. **Root cause of both failures is process, not difficulty:** neither design ever had a design pass — each was one paragraph written while fixing something else. **Not urgent** (renders are free and reproducible; the harm is coherence, not money or data loss). **TRIGGER: before the blob-addressing schema ships a migration** — a key shape is cheap to change while `video_artifacts` exists only in a spec, and expensive once rows exist. **Blocks:** deleting `video_artifacts_free_uq`, and closing the `check-sentinel-meanings.py` entry for `video_artifacts.generation_id`. | `schema/04_artifacts.sql` + `lib/pdf/` + `lib/html-doc/` + §4.0/§8 of the blob-addressing spec | M + design | (cloud) | pending — needs Phase 1 brainstorming |
| 26 | 🟠 **Which attempt ceiling governs a paid slot? `summary_max_attempts` = 1 or `jobs.max_attempts` = 5** — ADR-0007 deleted `reserve_artifact_slot`, and with it the **only per-kind attempt bound on the money path**. That function read `guardrail_config` — `summary_max_attempts` = **1** (a deliberate product decision, documented as such), `dig_max_attempts`, `max_serve_attempts` = 5 — and refused a further paid attempt past the bound; its assertion (*"with `summary_max_attempts`=1 a crashed summary slot is NOT retryable"*) was retired with it. The surviving bound is `jobs.max_attempts`, which **defaults to 5** (`supabase/migrations/0008_jobs_queue.sql:14`), so **deleting the artifact-layer bound silently promotes a summary from 1 paid attempt to 5**. ADR-0007's own Consequences assign this: *"The implementing slice must state which number wins; this ADR does not get to leave it implicit."* The implementing slice declined it in writing (`schema/04_artifacts.sql`, the ⛔ block over the deleted reservation: *"WHICH NUMBER WINS IS AN OPEN DECISION, not something this file settles"*), and it was tracked nowhere — found by the 2026-08-10 implementation review (M2), which is the only reason it is here. **It is a spec regression today, not a live money regression:** nothing outside `docs/` calls `record_artifact` or `reserve_artifact_slot` yet (`grep` over `*.ts`/`*.sql` outside `docs/` returns one comment in `tests/lib/blob-addressing-caller-contract.test.ts`). **Decide, do not default:** (a) keep 1 for summary and re-express the ceiling at the job layer (per-kind `max_attempts` at enqueue), (b) accept 5 uniformly and retire `summary_max_attempts` as a live knob, or (c) re-express the bound as a paid-attempt counter on the artifact slot. **Trigger — this must be closed BEFORE the schema ships to an environment where a worker calls `record_artifact` for a `summary`**, i.e. before task #44 (T5, code preconditions) puts a real caller on the money path. Until then it costs nothing; the day it ships it is a 5× spend ceiling nobody chose. | `schema/04_artifacts.sql`, `guardrail_config`, `lib/job-queue/*` | S (decision) + S (impl) | — | **OPEN — blocks T5 (task #44)** |
| 27 | 🟡 **A source generation is pinned for the LIFE OF THE WORKSPACE, not 90 days — decide whether GC ever releases it** — ADR-0007's T3 added a GC reachability check (`video_generations_collectable`'s second `not exists` over `video_artifact_sources`) so a summary generation that a paid `model`/`digDeeper` was built FROM is never swept out from under it. That is correct and it closed a real hole. What it is **not** is a retention delay: **MEASURED 2026-08-10** through the RPC — summary `gPs` recorded, model `gPm` recorded naming it, both superseded, `gPm`'s own generation swept — and `gPs` is *still* not collectable, because the provenance row cannot be deleted while its artifact lives, the paid artifact cannot be deleted at all, and §8's sweeper selects *through* the view. Only deleting the account clears it. **Consequence:** since `model` is generated on first serve, essentially every summary generation ever served is retained forever, and §8's *"a paid blob is collected 90 days after it stops being current"* does not hold for the dominant case. The schema comment promised the opposite (*"§8's retention clock … is what eventually releases it"*); found by the 2026-08-10 implementation review (H3). **The comment is now corrected and the pin is asserted as a characterisation in `schema/05_assert.sql`, so it is a stated fact with an instrument — but the retention decision itself is open.** **Candidate, measured to work:** scope the `not exists` to LIVE references, i.e. ignore sources whose referencing artifact's own generation is already `body_collected` (the same fixture then returns `gPs` as collectable). It is **not** applied here because it is a new retention rule — it decides when paid bytes become deletable, on the one path with no undo — and it leaves a second permanent pin standing anyway: a FREE render has no generation of its own, so `body_collected` is never true for it and its source stays pinned. **Trigger — decide before the §8 retention sweeper is implemented**, since the sweeper is the thing whose promise is currently false; the characterisation assertion goes RED the moment someone closes it, which is the intended signal. | `schema/04_artifacts.sql` (`video_generations_collectable`), `schema/05_assert.sql`, §8 | S (decision) + S (impl) | — | **OPEN — decide with the §8 sweeper** |
| 28 | 🟠 **A reserve TIMEOUT permanently strands 6¢ and burns an attempt — the bounded reserve is a SEQUENCE guard answered with a raw rejection** — found by the 2026-08-10 whole-branch review of #46 (M3), and it is a **delta this branch creates**, not a pre-existing hole. Before #46 the reserve RPC had no timeout: a slow-but-successful reserve eventually returned its token and settled normally. After #46 any reserve exceeding `SERVE_RESERVE_RPC_TIMEOUT_MS` (5s, and labelled *"PROVISIONAL — revise from observed p99"* — never measured) abandons a transaction that **already committed server-side**: `serve_owner_budget.spent_cents` and `spend_ledger.reserved_cents` were both `+=` by `magazine_est_cents`, `attempt_count` was incremented, and a `release_token` was minted that the client never received. **MEASURED-BY-READING that the strand is permanent** (`0020_reservation_release.sql:217-222,251-254,277-280`): a later reserve past lease expiry does `set reserved_cents = ..., release_token = <new>` — overwriting the lost token — while `settle_serve_model` matches only on `(owner_id, release_token)`, so no settle can ever reach the stranded 6¢. `serve_model_charge.reserved_cents` is SET (does not accumulate), but the two LEDGERS are `+=` and only a token-matched settle decrements them. **User-visible consequence:** five such timeouts on one document in a day → `attempt_count` hits `max_serve_attempts` → that document returns 503 for the rest of the day. **Why it was accepted for #46:** the alternative is the unbounded reserve, which is the double-charge the branch exists to fix; the state is named in the code (`'[serve-model] reserve timed out — possible empty paid lease'`) and logged at `console.error`. **Candidate fix, no new mechanism needed:** `settle_serve_model` matches on `(owner_id, release_token)`; a settle reachable by `(owner_id, doc_key, day)` for the case where the token was never received converts this rejecter into a **reconciler** using the table that already exists — `review-method.md`'s rule for a SEQUENCE guard is *reconcile, never a raw rejection*. **DECISION 2026-08-11 (user): DEFERRED — measure on a realistic system, not locally.** The 5s threshold is a TUNING knob and, by this project's own P/I/H lesson, the safe kind: it is visible, labelled `PROVISIONAL — revise from observed p99` in `lib/serve-budget.ts`, and generous against any healthy Postgres round trip. A local measurement would report ~5ms and prove nothing. **But note what tuning does and does not change:** the STRAND is structural — if the timeout fires at any threshold, the 6¢ is unrecoverable, because a later reserve overwrites the token and settle matches only on it. Tuning moves the FREQUENCY, never the severity. So the reason this is safe to defer is that it should be rare, not that it is harmless. **Trigger:** the first production/staging environment with real traffic — read `reserve_serve_model`'s p99 from the Supabase dashboard, or time the call in-app for a period. If it is anywhere near 5s, this becomes the next slice. | `lib/html-doc/serve-doc.ts:78-91`, `supabase/migrations/0020_reservation_release.sql` (`settle_serve_model`), `lib/serve-budget.ts` | S (measure) + M (impl) | (cloud) | **OPEN — measure p99 before deciding** |
| 29 | 🟡 **`supabase/migrations/` guards are invisible to this project's own guard-classification ratchet** — `scripts/check-guard-coverage.py` reads its population from `pg_catalog` for the blob-addressing `schema/` tables only, so migration-defined guards are never enumerated. VERIFIED 2026-08-10 during the #46 review: the new `guardrail_config_lease_ttl_covers_serve` CHECK produced **zero** rows in that script's output — its finding list is byte-identical with and without migration 0024 — and had to be classified by hand (SHAPE; reject is correct; mutation-verified against the live DB). `review-method.md` describes that ratchet as covering *"every constraint, unique index, FK and trigger, read from pg_catalog"*, which is true of the tables it looks at and not true of the database. **Not a defect of any one branch — a scope gap in a gate other branches are measured by**, and precisely the *"a rule that depends on remembering"* shape the ratchet was built to remove, one level up. **Trigger:** fold into the promotion slice that wires the schema gates into CI — the same work already has to decide which tables the catalog scan covers. | `scripts/check-guard-coverage.py`, `docs/review-method.md` | S | — | **OPEN** |
| 30 | 🟠 **`TRUNCATE` is granted to `anon` and `authenticated` on every money table — RLS does not cover it** — found by the 2026-08-10 round-4 review of #46. MEASURED on the live stack: `has_table_privilege('authenticated','public.ledger_audit','truncate')` is **true**, and identically true for `spend_ledger`, `serve_owner_budget`, `serve_model_charge` and `guardrail_config`, for BOTH `anon` and `authenticated`. ⚠ **CORRECTED 2026-08-19 — this row said `SELECT`/`INSERT` are "correctly denied (`false`)". They are not.** Re-measured against **prod** (`claude_ro`, read-only): `has_table_privilege('anon', …, 'select')` is **true** on all five — the GRANT exists. What is nil is the *effect*: all five carry `rls_on = rls_forced = true` with **0 policies**, so a session-role read returns 0 rows. The original sentence **conflated the grant with the outcome**, which is the exact confusion this row exists to name — and it is why the finding is real: **`TRUNCATE` is not an RLS-governed operation**, it is a table-level privilege, so for that one verb the grant *is* the outcome. Supabase's default `grant all on all tables in schema public` hands it to the session roles. `0020_reservation_release.sql:21` states the invariant as *"no policies → no session-client access at all"*, which is therefore **false as written** and is the premise the #46 settle-witness (migration 0025) leans on for durability. **NOT introduced by any recent branch** — it is a platform default affecting every table in `public`, and it long predates the money tables. **Not reachable through the application:** PostgREST exposes no TRUNCATE verb, so this requires a direct SQL session holding the `anon`/`authenticated` role — i.e. someone already past the connection boundary. **That bound is not a reassurance written here after the fact — it was already in the tree before this row was filed**, at `supabase/migrations/0025_settle_is_observable.sql:36`. **Fix as a CLASS, not per table:** `revoke truncate on <money tables> from anon, authenticated` in one migration, plus an assertion that enumerates the money tables from a list and fails when any of them grants TRUNCATE to a session role — otherwise the next table added re-opens it silently. Consider whether the same revoke belongs on every table with forced RLS. **Trigger:** the next security-hardening pass, or sooner if a direct-SQL session role is ever exposed to a client. | `supabase/migrations/0020_reservation_release.sql:20-22`, all money tables | S | (cloud) | **OPEN — class-wide revoke.** ⟳ **RE-MEASURED AGAINST PROD 2026-08-19** (`claude_ro`, read-only, SQL in a file): the TRUNCATE grant is **still true** for both `anon` and `authenticated` on all five money tables, and the row's `SELECT`/`INSERT` sentence was found wrong and is corrected above. Nothing changed and nothing new is exploitable — the re-measure was to establish whether the row still describes the live system, and it does. **The FUTURE half of the fix — `alter default privileges` plus a ratchet so the next table cannot re-open this silently — is split out as **#54**, which is where the durable work now lives; this row keeps the one-time revoke of the five tables that exist today.** |
| 32 | 🟠 **A data root's sync manifest is bound to ONE cloud — re-point it and sync silently uploads NOTHING, forever** — found 2026-08-11 while setting up the B1 round-trip on a fresh hosted project. Each data root keeps `<root>/<playlistKey>/.cloud-sync-manifest.json` (`lib/cloud-sync/manifest.ts:9`) recording what the last run saw on both sides. When a video is present locally and absent in the cloud, `sync-run.ts:618-623` branches on whether it is IN THE BASELINE: if yes → `report.removed += 1` and **no propagation** (deletes are M2b, deferred); if no → `copyAdditiveVideo`, upload. That is correct for the case it was written for — the owner deleted the video in the cloud, do not resurrect it. **Point an existing root at a DIFFERENT Supabase project and every video takes the first branch**, because the baseline says they were there and the new cloud has never heard of them. Measured: `removed: 10, created: 0`, **repeated identically on a second run**, with `errors: []` and **exit 0**. Nothing uploads, nothing warns, and the report reads calm. Resolved for the test by starting from a root with no manifest — that is a workaround, not a fix. **Reachable outside a test:** restoring prod from backup, recreating the project, migrating regions, or any second environment. **Decide in spec:** (a) detect a cloud-identity change — store the project ref in the manifest and refuse or re-baseline when it differs (cheapest, and turns a silent no-op into a loud one); (b) treat 'in baseline, absent remotely, AND the remote has no record of the playlist at all' as a first-sync rather than a deletion; (c) leave as-is and document. **Note (a) is the fail-closed shape this project keeps arriving at** — the run cannot do its job, so it should say so rather than report success. | `lib/cloud-sync/manifest.ts`, `lib/cloud-sync/sync-run.ts` | S–M + design | (cloud) | **OPEN — filed 2026-08-11** |
| 33 | 🟠 **`anon` holds EXECUTE on nearly every `public` function, and every migration's `revoke … from public` reads as though it does not** — MEASURED on prod 2026-08-11: **26 of 30** functions in `public` are executable by `anon`. Migrations across this repo write `revoke all on function f(...) from public; grant execute on function f(...) to authenticated, service_role;` — which reads as "anon excluded" and is not. `revoke … from public` removes the **PUBLIC pseudo-role**; Supabase's `pg_default_acl` separately grants EXECUTE to the **named role** `anon` at CREATE time (`f \| postgres=X/postgres anon=X/postgres authenticated=X/postgres service_role=X/postgres`). Proven pre-existing rather than introduced by any branch: `reserve_video_slot`, created in `0009` and untouched since, is equally anon-executable, as are `complete_job`, `fail_job` and `persist_summary`. `exec_sql` is the sole exception because it was explicitly revoked from anon. **NOT a breach today** — but ⚠ **CORRECTED 2026-08-19: this row said "these are `security invoker`", and a quarter of them are not.** Re-measured against **prod** (`claude_ro`, read-only, SQL in a file), the 26 anon-executable functions split **16 `SECURITY INVOKER` / 10 `SECURITY DEFINER`**, and the two halves are defended by *different* mechanisms — which the original single sentence hid: for the 16, RLS applies as the caller and `anon` is denied at the table (the designed defence, as filed); for the 10, **RLS is bypassed by definition**, and the only thing standing between `anon` and the body is that **8 of the 10 check `auth.uid()`**, which is NULL for `anon`. The remaining 2 were read in full and are benign: `handle_new_user` returns `trigger` (PostgREST cannot invoke it at all), and `reserve_serve_model_meta` returns `TABLE(secdef boolean, cfg text[])` and does nothing but read `pg_proc` to report whether `reserve_serve_model` is DEFINER — introspection, no writes, no money. **No exploitable path was found. Bound honestly:** *"references `auth.uid()`"* is a **proxy** for *"enforces ownership"*, not a proof — those 8 bodies were not read line by line, so what is established is that a defence is present, not that it is correct. **The defect is that the pattern LOOKS like a defence**, so a future `security definer` function written to the same template would be exposed with nothing flagging it. **Same class as backlog #30 (TRUNCATE granted to session roles), one layer out** — tables there, functions here, one platform default behind both. **Fix — SCOPE NARROWED 2026-08-19, and the narrowing is the point.** This row now owns only the **one-time** revoke: one migration removing EXECUTE from `anon` on the functions that exist today and should not have it. The other two clauses it used to carry — `alter default privileges` so new functions do not inherit the grant, and an assertion enumerating `public` functions that fails when a `security definer` one is anon-executable — are **split out as #54**, because the prod re-measure showed the live exposure is bounded while the **default** is not: revoking on 26 functions fixes 26 functions, and the 27th arrives anon-executable exactly as they did. Same split shape as #19/#20/#21 out of #17: what is measured stays here, what is structural moves. **Trigger:** the next security-hardening pass, or immediately before any new `security definer` function ships — and by then #54 should be the thing that catches it, not this row. | `supabase/migrations/*`, `scripts/check-docs.py` or a new assertion | S–M | (cloud/security) | **OPEN — filed 2026-08-11.** ⟳ **RE-MEASURED AGAINST PROD 2026-08-19:** the 26-of-30 count is **confirmed exactly**, and no exploitable path was found — but two claims in the row as filed were wrong or overbroad and are corrected above (the blanket "these are `security invoker`"; and the sibling row #30's grant-vs-outcome sentence). **Scope narrowed to the one-time revoke; the default + ratchet is now #54.** |
| 34 | ⚠️ **CORRECTED 2026-08-11 — NOT a live double-charge. The mechanism is real; the scenario was manufactured by the gate harness.** `loadSummaryForServe` reads the summary markdown with the same session-scoped store and the same principal and **fails closed at `409 "repair needed"`** when it comes back empty (`lib/html-doc/serve-summary-core.ts:66-67`). Both keys live under `${id}/${indexKey}/` and the storage policy grants on the first path segment alone, so a permissions fault kills the markdown read FIRST and the request ends before any reserve. The B3 harness read the markdown with the `service_role` client to isolate the model read, constructing a state the app cannot enter (markdown readable, model denied). **The charge was real; the path to it was not.** What was genuinely wrong: (a) `supabase-blob-store.ts` asserted a 404 "IS provable absence" and listed RLS denial as producing something else — both false, and it quoted the very error string that disproves it; (b) the protection was ACCIDENTAL — an ordering property no signature required, no test pinned and no comment mentioned, with `mdBody` optional so a new caller could reach the charging code with no upstream read at all. ✅ **FIXED**: comment corrected, `mdBody` now REQUIRED, and `tests/integration/serve-md-unreadable-no-charge.test.ts` pins the short-circuit (mutation-verified). Spec: `docs/superpowers/specs/2026-08-11-serve-path-proven-absence-design.md`. *Original entry, kept because the mechanism it describes is accurate:* **The serve path trusts an absence it cannot prove — an RLS-denied model read re-charges the owner** (found by M1.4 gate B3, 2026-08-11, against hosted staging; evidence in PR #76). `resolveMagazineModel`'s money guard treats `tryGet → absent` as proof the cached model is gone and falls through to `reserve_serve_model` + a fresh Gemini call. But **Supabase Storage returns a 404 for an RLS denial**, byte-identical to a genuine miss — measured: `{"message":"Object not found","status":400,"statusCode":"404"}` for both an object that exists behind a dropped policy and one that never existed. RLS makes the row invisible, so the API cannot say more. **Measured impact:** spend **6¢ → 12¢**, `attempt_count` 1 → 2, a second real Gemini call, then the write failed — i.e. the owner paid twice for a model already in the bucket. Repeats per view up to `max_serve_attempts`, then 503. **The repo already knows better:** `SupabaseBlobStore` declares `readonly provesAbsence = false`, and the **sync** path consults it (`lib/cloud-sync/sync-run.ts:697`) — which is the real reason gate B2 passed. The serve path never asks. `serve-doc.ts`'s own comment claims `tryGet` distinguishes *"5xx, timeout, RLS denial, transport error"*; it distinguishes three of four and misses the one it names first. **Scope, honestly:** only the RLS-denial class was exercised; a real 5xx/timeout carries a non-404 status and would pass. The guard is **narrower than its comment claims**, not useless. **Fix lead:** `serve_model_charge.attempt_count >= 1` already records that a model was written for that `doc_key`, so a later *absent* read there is corroborated-suspicious → treat as unreadable rather than re-charge. **Settle in spec before coding:** (a) is the charge row the right corroboration signal or is something more direct wanted; (b) does the same hole exist on the dig path that merges a cached envelope; (c) whether `isFresh`/the owner path needs the same treatment. **Money path → own branch, own spec pass, dual review.** ~~Re-run gate B3 against hosted staging as the acceptance check.~~ **Superseded:** B3's money clause tested a state the app cannot enter, so acceptance is `tests/integration/serve-md-unreadable-no-charge.test.ts` in CI. The staging project was deleted 2026-08-12. | `lib/html-doc/serve-doc.ts` (money guard) + possibly `lib/storage/blob-store.ts` seam | M + design | (cloud) | ✅ resolved 2026-08-11 — see the correction at the head of this cell |
| 35 | **The serve money guard depends on migration `0007`'s grant granularity, and `0007` does not say so** — found 2026-08-12 by the first `explain-diff` run (on PR #78), recorded as **ADR-0008**. `resolveMagazineModel` spends on `tryGet → absent`, which on Supabase means *absent OR RLS-denied*. It is safe only because `loadSummaryForServe` already read the markdown from the same folder and fails closed at `409` (`lib/html-doc/serve-summary-core.ts:66-67`) — **and that is evidence about the model only because the grant is on the owner path segment** (`0007_storage_and_rpcs.sql:12-15`, `split_part(name,'/',1)`). Narrow the grant below owner-folder granularity — per playlist, per object, membership-scoped — and reading the markdown stops implying the model is readable: the serve path reserves and regenerates a model already in the bucket (**6¢ → 12¢**, `attempt_count` 1 → 2, a second live Gemini call) with **every test green**. **Measured:** grepping `0007` for *serve/money/charge/reserve* returns only "preserves"/"preserved", and `split_part` appears in no other migration. **The inverse of ADR-0005** — there the `Dockerfile` looked *wrong* without the ADR, so someone went looking; here the migration looks *fine*, and the change that breaks it looks like better security. **The work:** annotate the policy in `0007` with a one-line pointer to ADR-0008 — **but settle first whether editing an APPLIED migration is safe here.** `0007` is one of 25 applied to prod; whether anything compares stored statements against the file was **NOT verified** (`CLAUDE_RO_DATABASE_URL` was unavailable in the session that filed this, and "cannot run" is a failure, not a pass). If editing is unsafe, the alternative is a check that fails when the policy's `split_part` clause changes without ADR-0008 being revisited. Code-side anchors already exist in `serve-doc.ts` and `supabase-blob-store.ts`. | `scripts/check-storage-grant-pin.py`, `.github/workflows/ci.yml` | XS–S + one verification | (cloud/money) | ✅ **RESOLVED 2026-08-12 — pinned, not annotated.** The blocking question was measured against prod: `schema_migrations` has **no checksum column** (`version`, `statements`, `name`), so editing an applied migration breaks nothing — **but `statements` retains comments** (0007 stores 15 statements, 6 with `--`, the policy statement among them), so editing would make the repo diverge from the record of what ran. The deciding argument was neither: **a comment does not fail.** Whoever narrows the grant edits the policy and its comment together, and the warning leaves with the thing it was warning about. `scripts/check-storage-grant-pin.py` pins the normalised statement digest, fails in CI naming ADR-0008, and carries a 6-case `--self-test` including the one that proves it fires (narrowing `split_part`'s segment). Ratchet contract still at baseline 4 |
| 36 | ✅ (was 🔴) **A non-ASCII video title destroys the paid summary — `slugify` keeps Hangul, Supabase Storage rejects the key** — found 2026-08-12 by the first real M3 acceptance run, against **prod, release v6**. `lib/slugify.ts:1` strips only `[^\p{L}\p{N}]`, and `\p{L}` is *any* Unicode letter, so a Korean title survives into the blob key. Supabase Storage then refuses it: `Invalid key: <owner>/<playlist>/_staging/<uuid>/003_돈-버는-방식은-정해져-있다-수익-모델-15종-다이제스트.md` — a string that appears **nowhere in this codebase**, i.e. the storage backend, not us. **The failure lands AFTER the money.** Gemini is called, the summary is generated, the write fails, and with `summary_max_attempts = 1` the job dead-letters on its first attempt. The reservation is correctly NOT released (`ever_metered = t` on the failed job vs `f` on the two that succeeded) — the durable guard from PR #22 refusing to refund work that genuinely cost money. So the accounting is right and the artifact is gone. **REPEATABLE, measured twice:** re-pasting the playlist created a second job for the same video which failed identically, spending again — jobs at `00:40:51` and `01:01:02`, ledger 450¢ → 606¢. **NOT an edge case:** in the 4-video test playlist, **2 videos were Korean-titled**; one died this way and the other escaped only by being rejected for duration first (1:04:16 > 30 min). The product supports `language: 'ko'` end-to-end and cannot store a file named in Korean. **BOTH OBVIOUS FIXES ARE WRONG:** stripping non-ASCII turns that title into the slug `"15"` (collisions, meaningless keys); keeping Unicode is what broke it. Transliteration, a hash suffix, or an ASCII-only key paired with a display title are the real options — **a design decision, deliberately not made here.** Also settle: should a dead-letter on an unstorable key refund, given the model call really happened? | `lib/slugify.ts`, the summary write path, possibly the key contract | M + design | (cloud/money) | ✅ **FIXED, MERGED (PR #104, squash `324ec77`), DEPLOYED (Fly v7) and VERIFIED AGAINST PRODUCTION — 2026-08-17.** ⭐ **The verification re-ran the ORIGINAL failure, not a substitute.** Video `wr4nCMUy1dk` (`돈 버는 방식은 정해져 있다, 수익 모델 15종 다이제스트`) had died in prod **twice** with `Invalid key`, both times `ever_metered = t` — the money had moved. On v7 the same video **`completed`**, appears in the library tagged KO, and its summary **serves**. The decisive observable is the physical key now in the bucket: `5a1df936-…/PLXX3HKP5ZNN0XbVDK0IkjESRuJFcJRxYR/003_=hc00SCQvLFMd1mWqZ8dodvO.md` — `003_` head preserved, `=h` marker, 22 digest chars, `.md` intact, and **the owner prefix and playlist key deliberately NOT encoded**, which is what keeps ADR-0008's serve money guard alive. That asymmetry was a claim in a document until this run; it is now a fact in the bucket. Encoder-marked objects went **0 → 1**. Money, read before and after via `claude_ro`: `spend_ledger` **2,142 → 2,292 = +150¢ reserved**, one new row; `actual_cents` stays 0, which is the known-deferred settle slice, not a miscount. **Original scoping, kept for the trail:** the encoder shipped ALONE (user decision). `lib/storage/supabase/encode-segment.ts` maps one logical path segment to a physical one (`SAFE` within `LIMIT` → byte-identical; anything else → `<head>=h<22 digest chars><ext>`), wired into `objectKey`, `deletePrefix` and `list` — the only three places that know both alphabets. No caller changes; no vault filename changes; **no migration** (the §4 prod gate was RE-RUN 2026-08-17 as `claude_ro`, read-only: 19 objects, **0** segments outside `SAFE`, 0 for each of the five characters Storage accepts but `SAFE` excludes). Decided by ADR-0009. Evidence: 10 encoder unit tests + 6 seam unit tests + 4 live-stack integration tests + 6 end-to-end. **MUTATION-CHECKED, not merely green** — reverting the seam takes the Korean and `⒈` cases RED, while the NFD and emoji cases pass BOTH ways because `slugify` strips those before Storage ever sees them (documented in the test, since the plan offered all four as encoder evidence and two are not). ⚠ **What this does NOT close:** the astral-surrogate slice in `slugify` (plan T3) is still open and is asserted as STILL BROKEN in `tests/integration/korean-title-e2e.test.ts`, so it fails loudly when fixed. The other twelve plan tasks come off the launch path and are re-filed on their own merits; `companionTransfer` (`sync-run.ts:475`) is the one with a MEASURED defect. **Prior state, kept for the trail:** spec v9, 2026-08-14, Phase 1 only; ZERO code written. 8 dual review rounds (16 halves) + a Phase 6 architecture review, all at `docs/reviews/spec-blob-key-encoding-r{1..8}-*` and `architecture-review-2026-08-14.md`. ⭐ **The design collapsed at v8 when the user asked "why does the cloud need ASCII-servable?" — it does not.** Two constraints had been welded into one word: *storable* (real, external, solved ENTIRELY by seam encoding) and *a single path component* (ours, a denylist). v5–v7's refusal, videoId repair, branded type and manufactured divergence were all machinery for a constraint that was not real, and are deleted. Round 8: Codex CONVERGED; both halves independently failed to break the collapse. Remaining: round 9 on v9, then plan, then implementation. ⛔ Deploy blocked on two grants the USER must run — see the spec §4.1. |
| 37 | ✅ (was 🟠) **A newly ingested playlist does not appear in the sidebar until a hard reload** — found 2026-08-12 in the acceptance run against prod v6. **Measured then: 3 playlists in the database, 1 in the sidebar.** ✅ **ROOT-CAUSED 2026-08-12, and the original wording of this entry was wrong in a way worth keeping visible.** It read *"never appears … unreachable without the URL"* and *"the failure mode is the product losing the work they just paid for"*. Neither is true: **nothing is lost and nothing is unreachable** — the summaries are generated, paid for and stored, and a reload of the same page lists all three. Re-measured against prod v6 the same day: a fresh load rendered `Business`, `CS146S The Modern Software Development` and `MS&E435 \| Spring 2026`, exactly the three rows in `playlists`, one owner, all titles non-null. So it is **client staleness within a session**, not a server read-path, RLS or null-title defect. Of the two theories this entry originally offered, *"caching"* was directionally right if read as client-side stale state and *"a source that is not `playlists`"* was wrong; what the query actually excluded is a **server-side** cause. Both were guesses made from the symptom before anyone opened the component. **Actual cause:** `PlaylistSidebar` is a SIBLING of the content pane in `CloudAppBody` and is never keyed by `playlistId`, so the `router.push('/?playlist=<new>')` that follows an ingest is a same-route client navigation — React reconciles the sidebar instead of remounting it. Its only fetch is `useEffect(…, [userId])`, and `userId` is stable for the whole signed-in session, so that effect runs exactly once, at sign-in. **The tell is an asymmetry:** `handleDeleted` already refetched the list, so the delete path was wired for freshness and the ingest path never was. **Related residue (folded in, NOT fixed here — still open):** a fully-rejected ingest still creates a playlist row — `22f36835` has 0 videos because all 9 exceeded `max_duration_seconds`. Decide whether a zero-video ingest should leave a row at all. | `components/cloud/CloudApp.tsx` + `components/cloud/PlaylistSidebar.tsx` | S (was estimated "S–M + investigation") | (cloud/UX) | ✅ **FIXED — PR #91 (`fix(#37)`)**, parent bumps a `refreshKey` on ingest success. **Took FOUR adversarial rounds, and every High in rounds 2–4 was introduced by the previous round's own fix** — the blob-addressing pattern, reproduced in a sidebar: per-effect `cancelled` flags → a half-covering sequence guard → a cancel check that ran after the write → a banner cleared by an older load. 8 regression tests, all asserting rendered output rather than call counts (a refetch whose result is discarded passes a count assertion). 13 mutations, every guard load-bearing; **2 mutations initially SURVIVED and both were gaps in the tests, not the code** — including a guard that was vacuous because its two operands came from the same stale closure. Residual complexity deferred as **#38**. **The 🟠 severity survived; the CHARACTERISATION did not** — *"losing the work they just paid for"* was wrong, and it is bad first-run experience rather than data loss. Residue above keeps its own line in this entry. |
| 38 | 🟡 **Extract the sidebar's load/refresh state machine into a `usePlaylistList` hook** — raised by the round-4 adversarial reviewer when asked directly whether the #37 design was worth its complexity; the answer was *"not as implemented"*. `components/cloud/PlaylistSidebar.tsx` now carries, inline, for a sidebar: two sequence counters (`startedSeqRef`/`appliedSeqRef`), an injected `isCancelled` predicate, a live `currentUserIdRef`, an `errorSeqRef` tagging the banner to the request that produced it, two effects, and two backfill triggers with different gating rules. **Every one of those was added in response to a measured defect** across four review rounds — which is precisely how a component becomes individually justified and collectively unreadable. **Proposed shape** (reviewer's, and it is a good one): a hook with ONE request API — `load({ reason: 'initial' \| 'ingest' \| 'delete' \| 'backfill', userIdAtStart })` — a single monotonic request id, and an error record tagged with the request and reason it describes, so "data freshness" and "is this failure still relevant?" become explicit and unit-testable without a DOM. **The argument FOR doing it:** four rounds proved this state machine is easy to get subtly wrong, and one guard shipped VACUOUS (comparing two values from the same stale closure, so it could never fire) — isolation is how that stops being luck. **The argument AGAINST:** the shipped code is correct and heavily covered (13 mutations, every guard load-bearing), so this is maintainability, not correctness. **Note the scope is smaller than it looks:** the reviewer also proposed remount-as-refetch (`<PlaylistSidebar key={refreshKey}/>`) and rejected it — it costs a visible loading flicker on every ingest and does not fix the null-title repair, which is sessionStorage-gated on the mount path. | `components/cloud/PlaylistSidebar.tsx` → new `components/cloud/use-playlist-list.ts` (+ move the 8 #37 tests off the DOM) | M | (cloud/UX) | **OPEN — filed 2026-08-12, deferred from PR #91 by decision.** Reviews: `docs/reviews/backlog-37-sidebar-refresh-codex{,-r2,-r3,-r4}.md` |
| 39 | 🟡 **Roadmap items have no permanent identity — the identifier IS the position, so renumbering silently rewrites what a reference means** — raised by the user 2026-08-13 while reading the PR #94 explainer. `check-roadmap-consistency.py:85` captures `([A-Z]?\d(?:\.\d+)?[a-z]?)` — `1.3`, `2.3b`, `M1.4`, `A4`. That is a **coordinate in a plan**, not a name: move the item and it becomes a different item, with no field that survives the move. **MEASURED, not hypothetical — three separate pieces of evidence.** **(a) It already cost a renumbering here.** `#31` in this very file records *"was #17 until 2026-08-11 — `17` had been assigned TWICE; renumbered because the fencing item owns every inbound reference"*. Read what that tiebreak concedes: the number **is** the identity — it simply had not been declared one, so it got issued twice. **(b) The references live where a rename cannot reach.** 11 files cite roadmap items by number, and **6 are memory files outside the repo**, plus PR bodies, commit messages and `docs/reviews/`. A repo-wide rename is therefore **permanently lossy across a boundary nothing can rewrite** — and worse than dangling, because a later reuse of `3.1` makes an old reference resolve silently to the WRONG item. **(c) The vocabulary is missing a word, and the roadmap already apologises for it in prose.** Deleting item 3.3 on 2026-08-12 required the note *"the deleted item is described rather than numbered here on purpose"*, because the checker reads a bare identifier as a pending claim. The model has **present** and **absent**; absent conflates *deleted*, *renamed* and *never existed*. It needs **retired**. **Proposed shape — two fields, not a replacement.** Keep the positional label (`M1.4` genuinely tells a human "milestone 1, step 4", and that ordering is why the file reads as a plan) and add a stable id beside it that never changes and is never reused. This is what `docs/adr/` already does (`0007` is identity, the title is meaning) and what this backlog does; the roadmap is the one layer that conflated them. Three checks then become possible that are impossible today: **id reuse** (the #17 failure, mechanically prevented), **dangling reference** (a doc cites an id nothing defines), and **retired-not-deleted**. ⚠ **A LARGER HOLE FOUND WHILE SIZING THIS, and it is the same piece of work:** only **23 of 58** checkboxes in the roadmap carry an identifier at all. `check-roadmap-consistency.py` audits **40%** of the file and reports clean — the other 35 items are invisible to it. Giving every item an id IS giving the checker full coverage, so do not treat these as two tasks. **Strong `docs/portable-practices.md` candidate** (measured twice here, project-independent): *separate the label from the identity in any document that other files cite.* Not filed there — that index is the user's call. | `docs/roadmap-to-launch.md` (every checkbox) + `scripts/check-roadmap-consistency.py` + a convention note in `docs/process-checklists.md` | M | (process) | **OPEN — filed 2026-08-13. NOT launch-blocking** (M3.1 and #36 come first); this pays back over months, and the evidence above is recorded now because in three months *"items need stable ids"* reads as a preference while *"#17 was assigned twice and 6 un-rewritable memory files cite these numbers"* reads as a measurement. |
| 40 | 🟡 **Package `explain-diff` + the localhost server as a Claude Code PLUGIN, so a new project gets it with one install** — raised by the user 2026-08-13: *"this feature is kind of side project that can be used for any new project."* Correct, and it is deliverable #2 (the reusable harness), not product work. **The practice is already portable** — `docs/portable-practices.md` §9 records the measured lesson (an agent that cannot RUN its own artifact ships every affordance untested; four defects in four rounds proved it). **This item is about the CODE.** **Three files, 646 lines, and they are nearly self-contained:** `.agents/skills/explain-diff/SKILL.md` (211), `scripts/explainer-serve.py` (360 — already fully project-independent; it serves `~/explainers` and knows nothing about this repo), and `.claude/hooks/suggest-explainer.sh` (75). **What is coupled to THIS project and must be parameterised or made optional (enumerated, not recalled):** (a) `CONTEXT.md` is read for vocabulary and cited for the *Storage Seam* naming caveat — must degrade to "if the repo has one"; (b) the ADR criteria are referenced by path into `.claude/skills/grill-with-docs/ADR-FORMAT.md`, a *different plugin* — inline the three criteria instead of depending on it; (c) "the dual adversarial review already owns implementation correctness" assumes this project's review culture — restate as a condition, not a fact; (d) `scripts/explainer-serve.py` is referenced by repo-relative path in two places — must resolve plugin-relative; (e) the skill quotes six project incidents (PR #84, #91, #92, #93, the backfill route) as evidence — **keep them, they are what makes the rules credible, but mark them as provenance rather than instructions.** **Recommended shape: ONE public repo for the whole harness, not one per tool** — and NOT this project's repo, which is private (nobody else could install from it) and would remix the product with the tooling that `docs/portable-practices.md` exists to keep separate. The shape was read off the installed `openai-codex` marketplace, whose entire manifest is one `plugins` array whose entries carry `"source": "./plugins/codex"` — **plugins live in SUBDIRECTORIES of a marketplace repo**, so one repo can carry many and each is installed individually: `kujinlee/claude-harness/` → `.claude-plugin/marketplace.json` listing everything under `plugins/`, then `plugins/explain-diff/{.claude-plugin/plugin.json, skills/explain-diff/SKILL.md, scripts/explainer-serve.py, hooks/suggest-explainer.sh}`, plus LICENSE and README. Anyone runs `/plugin marketplace add kujinlee/claude-harness` once and `/plugin install explain-diff@claude-harness`. **Why one repo:** later portable pieces (the ratchet-contract pattern, `sync-docs`, whatever else survives the two filters) become siblings under `plugins/` with no second `marketplace add` for anyone. One repo per plugin buys nothing and costs a manifest and an install step each. **FOLDED IN 2026-08-13 — the bidirectional side panel.** Today the loop is one-way: the page POSTs a question, a `Monitor` turns it into a session notification, and the **answer appears in the terminal**, so the reader context-switches away from the document they are reading. The version worth shipping to other projects closes it: `POST /questions` returns an **id**, the server gains `GET /answers?since=…`, and the page grows a right-hand thread panel that polls. Design it HERE rather than retrofitting, because the hard part is a contract, not a UI. ⚠ **The weak link, named in advance:** the panel only fills in if the agent writes each answer to `~/explainers/answers.json` instead of just replying in chat — **nothing enforces that**, and a conversation UI that silently stops updating is worse than none, because the reader watches a spinner instead of switching windows. Mitigations: have the Monitor event carry the question id so the answer has an obvious destination, and state the contract in the skill. Neither makes it mechanical, and that should be said plainly in the README rather than discovered. ⚠ **Second limit:** answers come from the LIVE session, so the panel is a *view* on one conversation, not an independent chat — close the session and it goes read-only. This is the opposite of the app's own `ask AI`, which hands the user to Gemini precisely because it does not need repo context. **Acceptance:** install it into a scratch repo with no Supabase and no `CONTEXT.md`, run `/explain-diff` on a real diff there, and confirm the explainer generates, serves, and round-trips a question — anything less is a claim about a neighbour, which is the exact failure this feature was built out of. **PUBLISHING IT (asked 2026-08-13: "can anybody install and use it?").** Mechanically yes, and the shape was read off the installed plugins rather than recalled: a **public git repo** carrying `.claude-plugin/plugin.json` (name, version, description, author, license, repository, and a `skills` array of `./skills/<name>/` paths — verified against `oh-my-claudecode`'s manifest) plus a `.claude-plugin/marketplace.json` if the repo hosts its own marketplace. Anyone then runs `/plugin marketplace add <owner>/<repo>` followed by `/plugin install explain-diff@<marketplace>`. Listing in Anthropic's `claude-plugins-official` is a **separate submission and not required** — a self-hosted marketplace is installable by anyone directly. MIT is the convention. ⚠ **TWO THINGS BLOCK "anybody" TODAY, both measured:** **(1) It is POSIX-only.** `scripts/explainer-serve.py` uses `os.fork()` (:232), `os.setsid()` (:247), `os.kill` (:153, :260) and `SIGTERM` — **`os.fork` does not exist on Windows**, so the server cannot start there at all. Either daemonise via `subprocess.Popen` with `CREATE_NEW_PROCESS_GROUP`, or state Unix-only in the README and fail with that sentence rather than an `AttributeError`. Python floor is **3.10** (`X \| None` annotations). **(2) Two hardcoded globals:** `~/explainers` and port `7391`. Fine for one user, a collision waiting to happen across many — make both overridable by env var. ✅ **Disclosure check already run 2026-08-13** (`grep` for project ids, hosts, keys, emails across all three files): **nothing sensitive** — the only `secret` hits are a `secret.env` test fixture inside the self-test. What WOULD become public is four project references: `CONTEXT.md` ×2, `PR #84`, `PR #91`, and the route path `app/api/playlists/backfill-titles`. Innocuous, but it is the user's call whether a public plugin cites a private repo's PR numbers. | new plugin repo + de-coupling the five points above | M | (process / deliverable #2) | **OPEN — filed 2026-08-13. NOT launch-blocking**; M3.1 and #36 come first. |
| 41 | 🟠 **M3.1-B — a prod read-only smoke against the DEPLOYED url** — the other half of roadmap **3.1**, which asks for the deployed URL and got a *local* stack in PR #96. Filed 2026-08-13 because until now it existed only in a chat message: a grep of `docs/` found **zero** mentions, so merging the A half with 3.1 unticked would have quietly retired the requirement. **Why A could not be B.** `/dev-login` is **404 in prod by design** — `lib/supabase/dev-login.ts` requires both `DEV_LOGIN_ENABLED` and a local Supabase URL and fails closed on either — and Google OAuth cannot be driven by Playwright. So an automated prod journey would need the gate opened (a security regression) or a session captured by hand. **Shape:** capture `storageState` once by signing in manually, commit nothing, and assert a **read-only** journey — signed-in shell renders, sidebar lists, a summary page loads, a download returns bytes. **No ingest and no HTML magazine render**, because both spend: PR #96 measured the serve path reserving 12¢ on first view. **Accepted cost:** the captured session expires, so this is semi-manual forever and can never run unattended. That is why A was built first — A is the regression net, B is a deploy smoke. **Open question this item must answer, and it is the user's:** *does M3 close on A, or wait for B?* The roadmap says *"M3 done = 3.1 and 3.2 both verified against the same deployed release"*, and A is verified against localhost. If M3 closes on A, that sentence needs amending to say what actually happened. | `tests/e2e/` + a captured storageState kept OUT of the repo | S–M | (cloud/test) | **OPEN — filed 2026-08-13. ⭐ NO LONGER GATES M3.** The open question in this entry — *does M3 close on A, or wait for B?* — was **ANSWERED BY THE USER 2026-08-13: M3 closes on A.** Roadmap 3.1 is ticked and the "M3 done =" sentence is amended in place to say the two halves are verified against **different things** (3.2 manual vs deployed v6; 3.1 automated vs a local stack), rather than pretending the original requirement was met. So this item is a **deploy smoke, a follow-on** — worth doing, not blocking. The gap it covers is named in the roadmap rather than hidden by the tick: no automated journey runs against production, and none can. ⏳ **DESIGNED AND BUILT 2026-08-19 — spec [`2026-08-19-prod-readonly-smoke-design.md`](superpowers/specs/2026-08-19-prod-readonly-smoke-design.md), and the user DECIDED it is a GATE (a deploy step), not an optional instrument.** Six checks, four pre-flight refusals, `npm run test:e2e:prod`. **Verified against release v7 with no session captured: 3 pass (release recording, the auth-lock probe, the money guard), 3 report NOT RUN naming the missing session** — which is the behaviour that mattered most, since a check that cannot reach its subject must never read as green. ⭐ **The suite also arms the FIRST money guard production has ever had**: `claude_ro` bypasses RLS and holds zero write grants, so the spend ledger can be bracketed read-only. Two defects found by running it rather than reading it: `describe.serial` abandoned the three session-free checks after the first NOT RUN, and the pooler's TLS chain is rooted in a private Supabase CA — now pinned from Supabase's published download, after `sslmode=require` in the credential URL was found to silently override the `ssl` config **and to have been non-verifying all along**. **REMAINING, and it is the human's:** `npm run prod:session` — one Google sign-in, captured once. |
| 42 | 🟡 **`middleware` is deprecated in favour of `proxy` — Next says so on every dev-server start** — `⚠ The "middleware" file convention is deprecated. Please use "proxy" instead.` Observed repeatedly while building the cloud e2e (2026-08-13); filed because **`AGENTS.md` opens with "Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices."** and this one has been printing unheeded. **Not cosmetic.** `middleware.ts` is the auth gate: it calls `supabase.auth.getUser()` to refresh the session, provisions anonymous users, and redirects an authenticated user away from `/login`. A convention rename that touches the request pipeline touches **every** protected route, so this is read-the-guide-first work, not a rename. **Do first:** read `node_modules/next/dist/docs/` on the proxy convention and establish whether it is a rename, a different execution model, or a different position in the request lifecycle — the three have very different blast radii. **Then** decide urgency: a deprecation warning is not a breakage, but the guide will say when it becomes one. | `middleware.ts` → `proxy.ts` (+ every protected route indirectly) | M + read the guide first | (infra) | **OPEN — filed 2026-08-13. NOT launch-blocking** |
| 43 | ✅ (was 🟢) **Un-skip rung 4 of the cloud e2e — the magazine HTML render** — `tests/e2e/cloud-journey.spec.ts` skips it, and the skip is a **cost decision, not a defect**: the setup pre-seeds `models/{base}.json` so the resolver should find one and skip Gemini, the blob **is** written (verified in `storage.objects`), and it regenerates anyway. Each guess at why costs 6–12¢; PR #96's debugging put **24¢** through the local ledger before this was understood. **The contract, read AFTER the skip was written, so this is now bounded rather than open:** `serve-doc.ts:78` calls `readFreshMagazineModel({…, titles})`; `read-model.ts:37` gates on `isFresh(existing, titles)`; and the comment at `read-model.ts:45` states plainly that `sameTitles` compares the envelope's **`sourceSections`** against the caller's **`titles`**. The setup writes `sourceSections: ['2. Encoder']`; the resolver's `titles` almost certainly drop the numeric prefix. **Do it by reading what derives `titles`, not by trying values** — trial and error on a money path bills per attempt, which is the whole reason the step is skipped. **Guard against a false green:** rung 6 already asserts the ledger did not move, so a "fix" that merely regenerates faster fails the suite instead of passing it. | `tests/e2e/cloud.setup.ts` | XS once the contract is read | (cloud/test) | ✅ **DONE 2026-08-13 — PR #98.** The fix was ONE TOKEN: `sourceSections: ['Encoder']`. `parse.ts:53-55` splits the ordinal off the heading into a separate `numeral` field, so the titles `sameTitles` compares against never carry the number. Rung 4 is un-skipped and the run-level money guard confirms it renders for **free** — 7 passed, ledger unmoved. ⚠ **THE UNCOMFORTABLE PART: this entry already contained the right answer.** It said *"the resolver's `titles` almost certainly drop the numeric prefix"* and *"do it by reading what derives `titles`"* — written 2026-08-13, then left unread while four Codex review rounds ran over the same files without noticing. It took the **Claude half of the dual review** — the half that had been skipped — to convert a correct hypothesis sitting in the backlog into a fix. Filing a good diagnosis is not the same as acting on one, and a backlog is not a reviewer. |
| 44 | ✅ (was 🟡) **The cloud e2e never mounts the main pane — `PlaylistLibrary` and everything under it are untested in a browser** — found by the Claude half of the M3.1 dual review, 2026-08-13. Every rung opens `/` with no query string, and `CloudAppBody` renders `PlaylistLibrary` only when `searchParams.get('playlist')` is non-null (`CloudApp.tsx:98`). So `PlaylistLibrary`, `VideoList`, `FilterBar`, `IngestProgressBanner` and `ScopeProvider` are **never mounted** by the suite. The sidebar link is asserted visible three times and **never clicked**, so "the link leads to a working library view" is unverified. The video the setup works hardest to seed is read over HTTP by rung 5 and never displayed. Rung 3's own `router.push('/?playlist=…')` lands on a playlist with zero videos, so even incidentally the list is only ever seen empty. **Also absent at browser level:** playlist deletion (`DeletePlaylistDialog`), sharing (`ShareDialog`), dig-deeper serving, PDF. **Why this is 🟡 and not 🟠:** none of it is broken — M3.2 verified these by hand against release v6 on 2026-08-12 — it is unguarded, which is a regression-net gap rather than a defect. **The reason it is filed at all** is that the spec header claimed the *only* missing coverage was the magazine HTML render, and a completeness claim in the one comment whose job is to bound a gap is worse than no claim. That sentence is now corrected in place. **Shape:** add a rung that clicks the sidebar link, asserts the library renders the seeded video, and opens it — no money, since the model is now seeded correctly (see #43). | `tests/e2e/cloud-journey.spec.ts` | S | (cloud/test) | ✅ **DONE 2026-08-20 — PR #120.** `tests/e2e/cloud-library.spec.ts` now mounts the pane, clicks through, and asserts the seeded video renders; **9/9 green**. It replaced a `describe.skip` stub whose two stated blockers (a `STORAGE_BACKEND=supabase` web server, a captured session) had **both been built months earlier** — nothing connects "the thing you were waiting for now exists" back to the file that was waiting. ⭐ **Two defects surfaced the moment anything finally rendered the list, and neither was reachable from the route level.** (a) `CloudApp.tsx:271` filters on `v.overallScore >= filters.minScore`, and **`undefined >= 0` is `false`** — an unscored video is invisible even at the "All scores" default, and the empty state blames the user's filters. Every route fixture omitted the score because routes never filter. Raised for a decision as a possible product defect; the fixture now sets one. (b) The journey's sign-out rung revoked the **shared** session, killing every spec that sorted after it — and a fresh browser *context* did **not** fix it, because `supabase.auth.signOut()` defaults to **global scope** and revokes every session the *user* holds. It now uses a throwaway account. |
| 45 | 🟡 **M3.1 round-5 residue — the Mediums and Lows left unactioned when PR #98 merged** — round 5's two Highs were fixed (the guardrail pin deleted, `sourceSections` derived); these were presented for a decision rather than fixed, per `review-method.md:107`. **(a) UI mode silently tests the previous session's owner.** `globalSetup` runs once per SESSION on the test-server path, so the fixture is not deleted between runs and `readFixture` returns an earlier session's owner; since the suite never deletes owners, every rung can pass against a database nobody seeded this run. The branch's answer was to narrow the claim to the CLI and say UI is unsupported. **The reviewer argued the other side and it deserves a real decision:** detection is the deliverable — a `runId` in the fixture that `readFixture` refuses to mismatch is three lines, redundant on the CLI and the only signal on the path where deletion never happens, and "detect but not prevent" is the standing of every other guard here including the money guard itself. **(b) The CLI-only warning is in the wrong file** — `tests/e2e/cloud-global.ts`, which a Playwright-UI user has no reason to open; it belongs in the spec header and next to `package.json`'s `test:e2e:cloud`. **(c) A rung-2 flake is recorded ONLY in a commit message** — it failed once and passed on an identical re-run, cause unknown, in a `describe.serial` suite where rung 2 gates rungs 3–6. A commit message is not a place findings survive. **(d) Rung 4 now gates rungs 5 and 6.** It is the only rung whose outcome depends on `guardrail_config`, on the day's `spend_ledger`, and on an external API being configured, and un-skipping it put the two most deterministic rungs — the md download and the sign-out session check — behind it. Moving it after rung 6 costs nothing. **(e) `expect(res?.headers()['x-magazine-stale']).toBeUndefined()` is vacuous when `res` is null**, and is non-vacuous today only because the `toBe(200)` two lines above fails first — an ordering dependency, not a guarantee. It is also still unmutated: no run has observed that header present. | `tests/e2e/*`, `package.json` | S | (cloud/test) | **OPEN — filed 2026-08-13, residue of PR #98** |
| 46 | 🟢 **Normalize the title with NFKC before `slugify`, so the mint path stops emitting characters that fold into path syntax** — surfaced 2026-08-14 by the user, reading the backlog-#36 round-11 Blocking. **The defect it dissolves:** `⒈` (U+2488 DIGIT ONE FULL STOP) is a *single* character Unicode classifies as a number, so `slugify` keeps it — and it NFKC-folds to `1.`, which put a `.` at the end of a slug and manufactured a `..` when the `.md` extension was appended. 21 such characters (U+2488–U+249B, U+1F100). If `slugify` normalized first, `⒈` → `1.` → the `.` is not alphanumeric → `-` → trimmed, giving the **cleaner** `003_lesson-1.md`. **MEASURED 2026-08-14, both halves of the question:** ① **6,672,384** slug outputs swept — `slugify(NFKC(title))` is **NFKC-stable in every case**, including characters landing on and straddling the 60-char `.slice()` boundary and lone surrogates from a cut astral pair. That makes the serve guard's fold a **provable no-op** for anything the mint path produces, which retires the whole class of *"the folded form differs from the real form"* surprises rather than the one instance. ② Blast radius **4,715 of 1,112,064 codepoints (0.424%)** — and **0 of 11,184 Hangul syllables, 0 of 20,992 CJK ideographs**. What changes is typographic compatibility forms: math alphanumerics (𝐀), CJK squares (㎏), enclosed numbers (⒈), ligatures (ﬁ), full-width ASCII. **✅ PROD MEASURED 2026-08-14** (the grants landed that day): `select count(*) filter (where normalize(name, NFKC) <> name)` over the `artifacts` bucket returns **0 would-change, 0 non-ASCII, 19 total** — read-only as `claude_ro`, exit 0, reachability asserted first. So this change would rename **nothing** currently in prod. Local evidence agrees: 12 vault summaries and 1,354 local Storage objects, all ASCII, none change. **⛔ Still NOT MEASURED — treat as NOT RUN:** the user's real Obsidian vault, because only 1 of 4 candidate roots existed on the machine this ran on. That is the population this change actually renames, since `slugify` feeds vault filenames. **Why a separate slice and not part of #36:** `slugify` is shared with the local path, so this changes **vault filenames**, and re-ingesting an already-ingested affected video would diverge from its existing file — the same migration shape #36's own §4 gate exists to answer. It needs its own spec, its own no-migration gate, and its own review. **⚠ It does NOT replace #36's guard fix.** The two cover different populations: `slugify` guarantees what *we* mint; the guard defends the **adopt** path, which takes vault filenames verbatim from `readdir` and can therefore receive anything a human types. Shipping only this one leaves the adopt hole open. | `lib/slugify.ts` + its own spec and no-migration gate | S + design | (cloud) | **OPEN — filed 2026-08-17.** ⚠ **This row was given a CLOSED marker in error on 2026-08-18 and reverted 2026-08-19** — see the ⚠ note at the end of the Item cell. `lib/slugify.ts` contains no `normalize`/NFKC call, so the work described here has NOT shipped. |
| 47 | 🟡 **A knowledge graph over document fragments, so prior work is FOUND rather than remembered — deliverable #2 (the reusable harness), same category as #40** — raised by the user 2026-08-15, citing Karpathy's *second brain*: *"proper book-keeping practice to maintain various relations among fragments of docs may be a good portable project tool."* **THE MEASURED FAILURE THIS ANSWERS.** Backlog #36 spent **thirteen review rounds and a design review** rediscovering a decision that was already on disk in three places: the model blob was originally keyed by `id` (`2026-07-02-stage-1c-supabase-adapters-design.md:160`), the drift to `base` **was caught** and filed Minor under a *"Carry-forward → Task 7"* heading with no task id (`task-1f-a-6-materialize-helper.md:19`), and the destination was independently re-derived in full and parked (`2026-08-03-stable-blob-addressing-design.md:179-184`). Nobody searched. It cost a High on a money path. **WHY `scripts/prior-art.py` (built 2026-08-15) IS NOT ENOUGH.** It is grep with document-class ranking. It answers *"where does this string appear?"* and therefore **requires you to already know the term** — but the #36 session did not know `MODEL_KEY` was the term until after the design pass. It cannot answer *"what relates to this?"*, *"which decisions were later reversed?"*, or *"which deferrals never landed anywhere?"* Keep it: it is cheap, self-testing, and it works when you do know the term. This item is the layer above it. **THE DESIGN CONSTRAINT THAT DECIDES WHETHER THIS IS WORTH BUILDING: derive the edges, do not maintain them.** This repo's own rule (`hardcode-only-what-fails-loudly`) applies with force — a hand-maintained graph over 853 documents goes stale silently and is then **worse than grep**, because it looks authoritative. Only build edges a script can extract and re-extract: doc→doc citations, doc→`file:line` code references (reviews are full of them), doc→identifier, spec→version lineage, decision→superseded-by, and **finding→destination (task/backlog id)**. **The highest-value query is a DANGLING EDGE, not a path:** *which deferrals name no destination?* — that is exactly the #36 loss, and it is mechanically detectable. The rule added to `process-checklists.md` on 2026-08-15 (*a carry-forward that names no destination is a note, not a carry-forward*) is a prose statement of a graph property; this item makes it checkable. **SHAPE, smallest first:** (1) an extractor producing a JSONL edge list, re-derivable from scratch so it can never drift; (2) a dangling-deferral report — the ratchet; (3) neighbourhood query (*"show me everything within 2 hops of `MODEL_KEY`"*); (4) only then, if it has earned it, embeddings for the *"I don't know the term yet"* case. **Stop after (2) if (3) does not pay.** ⚠ **Do not start this during a launch-blocking slice.** It is harness work; #36 is the last launch blocker. | a fragment index over `docs/` + a dangling-deferral ratchet | L + design | (process / deliverable #2) | **OPEN — filed 2026-08-17.** Harness work, explicitly not to be started during a launch-blocking slice. |
| 48 | ✅ (was 🟡) **A TRIGGER for unattended work, because a stated next action is not a scheduled one — deliverable #2 (the reusable harness)** — raised by the user 2026-08-17: *"don't you think you need better mechanism to trigger rather than 'Doing item 1 now'? Some self evaluation of status mechanism seems to be broken."* **THE MEASURED FAILURE — FIVE instances, same shape** (count updated 2026-08-21): an overnight AFK gap; stopping after announcing a review half would be read; and 2026-08-17 *"Nothing needs a decision from you this second… I can carry on"* followed by no action; ⭐ **and (4)+(5) on 2026-08-19/20 — backlog #44 and #53, both closed with the sentence *Next action: I will start #44 now*, and neither started.** **4 and 5 are the sharpest of the five:** the announced work was small, unblocked and immediately executable, so nothing external explains the stop — which isolates the defect to ORDERING rather than to capability or permission. **Status assessment was CORRECT every time** — the remaining work was listed accurately, in order, with paths. The defect is that STATING the next action was treated as SCHEDULING it. Nothing invokes an agent that has stopped, so the declared action never happened, and **there is no feedback signal distinguishing "turn ended with work parked" from "turn ended because done"** — which is why it never self-corrected. **RULED OUT 2026-08-17 — it is not the permission mode.** The user switched bypassPermissions → auto mode and asked whether that explained it. It does not: the two auto-mode denials this session did NOT end a turn (work continued, commits `c18aebb`, `093e61a`), and the three declare-then-stop instances involved ZERO denials — the next action in each was a markdown edit auto mode permits freely. Blocked and stopped are uncorrelated in the record. **OPTION A — `/loop` (RECOMMENDED, needs no permission).** `/loop` with no interval self-paces and grants `ScheduleWakeup`, a real scheduler: "carry on later" becomes a wakeup with a delay and a stated reason. ⚠ **Neither the user nor the assistant has ever run it** — do NOT write it into `dev-process.md` until measured, per this repo's own rule that a practice is codified when measured, not when it sounds right. **TRIAL IT ONCE** on a real wait (a dispatched review round is the right shape) and measure three things that would make it a bad default: ① token cost per wake, since an unattended night is exactly the unwatched case; ② interaction with `.claude/plan-gate-pending` — a loop that wakes and bounces off an armed gate is noise, not work; ③ whether it stops cleanly when the work genuinely needs a human. If it holds, add ONE line to `dev-process.md`'s Conditional AFK section: *an unattended stretch starts with `/loop`; the loop is the trigger, prose is not.* **⚠ SCOPE LIMIT:** `/loop` fixes the UNATTENDED case only. It does nothing for declare-then-stop during a normal interactive session, because no loop is running. **OPTION B — a `Stop` hook (NOT RECOMMENDED, needs USER action).** A `Stop` hook can read the final assistant message and `exit 2` to refuse the stop — same shape as `.claude/hooks/block-implementation-before-plan-gate.sh`, and the same lineage as *"before adding a rule, ask whether it can be a script."* **It was written and BLOCKED by the auto-mode classifier, twice** — once at `.claude/hooks/`, once at a scratch path, so the classifier judged CONTENT (a script suppressing the agent's own stop), not path. **Nothing reached disk; the only copy died with that transcript.** The assistant did not retry, and agrees with the block: an agent quietly installing code that makes itself harder to stop is a bad thing to discover afterwards. **WHY IT IS NO LONGER RECOMMENDED (a correction to the assistant's own earlier "I'd install it"):** the discriminator is KEYWORDS OVER PROSE, so it cannot tell a promise from a *description* of next steps — and a good answer to *"what is current status"*, which this user asks routinely, says "the next action is X" and would trip it. A guard that fires on correct behaviour teaches you to disable it, and a disabled guard is worse than none because it still looks like coverage. It also treats the symptom (phrasing) rather than the condition (work parked with nothing in flight), which it cannot observe. **IF BUILT ANYWAY:** allow ONE narrow path, not `.claude/hooks/*`; gate on `stop_hook_active` so it can never loop; ALLOW-list handoffs (a trailing `?`, "your call", "human gate", "blocked on"); document the `BLOCK_STOP_INTENT=0` escape hatch where it will be found *before* it is needed; and narrow the fire condition to *declared intent AND an open task AND nothing in flight*. **DECISION OWED:** trial `/loop` → then either write the `dev-process.md` line, or record here that it did not pay and close this item. Discard Option B unless declare-then-stop recurs OUTSIDE an unattended stretch — that recurrence is the evidence that would justify its false-positive rate. **⚠ THIRD INSTANCE, 2026-08-17 12:43, and it is the one that settles the argument.** The turn that ended *"dispatching round 6 next, and I'll say plainly if it produces the same class again"* dispatched nothing — verified: no `plan-cloud-blob-key-encoding-r6-*` file, no prompt written. It recurred **in the same message that described the failure and undertook not to repeat it**, roughly two hours after the fork recommended AGAINST the Stop hook on false-positive grounds. That argument — keyword matching cannot distinguish a promise from a status report, and the user asks for status routinely — still stands and is not withdrawn. What changed is the other side of the ledger: the recurrence rate is **3 of 3** unattended-continuation turns today, and self-correction has now failed with the failure explicitly in context, which is the strongest possible evidence that no amount of intention fixes it. **Reopen the option with the narrower fire condition** (declared intent AND an open task AND nothing in flight — the last conjunct alone would have allowed every status report today, because each was written while a review was running). Decide it against the `/loop` trial, not before. | `/loop` trial + the Stop-hook decision | M + decision | (process / deliverable #2) | ✅ **CLOSED 2026-08-21 — `/loop` IS THE MECHANISM; the Stop hook is DISCARDED.** This row said *decide against the trial, not before it*, and the trial ran on 2026-08-21 in two halves that tested different things. **#53 — THE LOOP REFUSED:** it read the row, found the item's own build gate said do not build, verified both triggers and stopped without a PR. Correct — and it also exposed that #53's trigger could never fire (retired in PR #122). **#54 — THE LOOP RAN TO COMPLETION:** read the row, designed the ratchet, built it, caught its own fail-open parse, mutation-tested it, ran the gates and opened PR #123 — **no prompting at any step.** That is the falsifier this row asked for, and it passed. ⚠ **BOUND, stated rather than glossed: `/loop` is USER-INVOKED, so it covers PLANNED absence only.** It cannot cover the unplanned case — stopping mid-thread while nobody knows to start a loop — because by definition nobody knew. That half is covered separately and behaviourally: **act-then-report** (do the work before the message, or say *not started, say go*), plus **any named-but-unfinished work becomes an `in_progress` task** — which is precisely the feedback signal this row identified as missing, since a parked declaration now appears in the task list the next turn reads. **STOP-HOOK VERDICT: NOT BUILT, deliberately.** It inspects the sentence rather than the gap and could be satisfied by rewording while still doing nothing. Revisit only on a sixth instance — one is enough to show the behavioural half is not holding. |
| 49 | 🟡 **`scripts/check-doc-citations.py` — resolve every `path:line` a plan or spec cites, because EIGHT have now been wrong in one document — deliverable #2 (the reusable harness)** — **THE MEASURED EVIDENCE, all from `docs/superpowers/plans/2026-08-15-cloud-blob-key-encoding.md`.** Round 3 (Codex, Low) found ONE wrong citation: `helpers/cloud.ts:132` should be `:131`. v4 fixed that one and rechecked none of its neighbours. A mechanical sweep of all 19 citations in Task 0's inventory then found **six** wrong (`spendLedgerTotal` 157→153, `cloudVideoRecord` 468→471, `localVideoRecord` 473→476, `seedVideo` **378→296**, `seedFreshModel` 78→79, `putBudget` 21→18). The round-4 Claude half independently found a **seventh** the sweep missed — the `Ctx` declaration of `syncDeps`, cited `:66`, actually `:69`, because `:66` is a mid-docstring line. The round-5 Claude half found an **eighth** — `cloudPrincipal` cited `:59`, actually `:58`. **Two independent sweeps each missed what the other caught, which is the argument: this is not a care problem.** ⚠ **`seedVideo` is the one that shows the cost**: 82 lines off lands an implementer inside a different function, and nothing about the wrong number looks wrong. **SHAPE:** parse ``` `path:line` ``` and ``path.ts:NN`` spans out of `docs/**/*.md`, resolve each against the working tree, and FAIL when the cited line does not contain a declaration of the symbol named nearby — or, where no symbol can be inferred, when the line is blank or is a comment continuation (the `:66` and `:157` failure shape). **Must have `--self-test`**, like the four existing ratchets. ⚠ **It can only check what the tree still contains** — a citation into a deleted file must fail loudly, never be skipped, per CLAUDE.md's *"cannot run" is a FAILURE*. **DO NOT BUILD DURING #36** — it is harness work and #36 is the last launch blocker; the eight instances above are already corrected by hand. | new `scripts/check-doc-citations.py` (+ `--self-test`) | S–M | (process / deliverable #2) | **OPEN — filed 2026-08-17.** The eight measured instances are already corrected by hand; the ratchet is what stops the ninth. |
| 50 | 🟡 **Refine the `/brief` skill — shipped 2026-08-17 as a deliberate draft — deliverable #2 (the reusable harness)** — **WHY IT EXISTS.** The user, returning after several hours: *"I want to have some tool to follow your work as your text proses are hard for me to follow."* That day produced 9 commits, 5 adversarial review halves, an architecture review and a scope finding that invalidated 12 of 16 plan tasks — delivered as a running series of prose messages. Each was accurate; together they were unfollowable, and the one thing the user needed (a decision was waiting on them) sat in paragraph four of the fifth message. `/explain-diff` did not fit: it explains a CHANGE, and that day changed no source file. **KNOWN GAPS, recorded in the skill itself so they are not rediscovered:** (1) **no `--self-test`** — the four ratchets in `scripts/` each have one; `/brief` has nothing proving it read what it claims to have read, which for a status tool is the `a-checklist-item-can-be-an-unfalsifiable-guard` shape; (2) **the ground-truth command list is hardcoded** and will drift — per `hardcode-only-what-fails-loudly` a MISSING file fails loudly but a RENAMED one fails silently by returning nothing, so the skill must report empty results rather than skip them; (3) **no staleness bound** — the page is true at its footer timestamp and says nothing about how fast it decays; (4) **unmeasured when no decision is pending** — the shape it produces then is untested, and the risk is that it manufactures a decision to fill the slot. **THE REAL TEST:** does a returning human reach the right next action without reading the transcript? Measure that before adding features. ⚠ Related: **#40** (package `explain-diff` + `explainer-serve` as a plugin) — `/brief` is the third member of that family and should ship in the same package. Note the two formats are complements, not rivals: an explainer is DURABLE and offline (`~/explainers/`, readable in five years, no network); a brief is LIVE and reachable (a hosted URL, no local server, but Artifact CSP forbids the `explainer-serve` POST channel, so no Ask button). ⭐ **ENHANCEMENTS REQUESTED BY THE USER 2026-08-17, after using it to make a real decision:** **(a) ✅ DONE 2026-08-18 — auto-refresh SHIPPED.** The user hit it a second time (*"Can this be refreshed from session rather than user initiate the page refresh?"*) and it is now built: `explainer-serve.py` gained `GET /_rev?p=<page>` (returns `mtime_ns:size` — **size is load-bearing**, a rewrite inside one clock tick has an identical mtime) plus a live-reload client the SERVER INJECTS at send time. **Injected, not authored into the page, on purpose:** the tray is lifted verbatim page-to-page by `brief-compose.py`, so putting it there would reach only pages generated afterwards and would re-enter the hand-copied-code failure measured 2026-08-17. Injection reaches every page including those already on disk, and `brief-compose.py` did not change at all. `file://` still gets nothing, so the self-contained artifact keeps working — live reload is a property of being SERVED. **It refuses to reload while `#qbox` holds a draft or has focus**, so it cannot eat a half-typed question, and it preserves scroll position across the reload via `sessionStorage`. **DRIVEN IN A REAL BROWSER, all three behaviours:** reloads on change; does NOT reload with a 53-char draft in the box (a `window` sentinel and the draft both survived); catches up once the draft is cleared. Self-test 25 → 32 cases. ~~(a) auto-refresh the page when an answer is posted~~ — today the reader must reload to see a reply, and the whole point of the Ask loop is that the answer lands where the question was asked; the page is already served over HTTP by `explainer-serve.py`, so a poll of the file's mtime or an SSE endpoint are both open, and the cheap version is a conditional GET every few seconds against the page's own URL. (b) whatever else surfaces from real use — the acceptance test stays *does a returning human reach the right next action without reading the transcript?* | `.claude/skills/brief/SKILL.md` + `scripts/brief-compose.py` + `scripts/explainer-serve.py` | M | (process / deliverable #2) | **PARTIALLY DONE — filed 2026-08-17.** ⚠ **Given a CLOSED marker in error on 2026-08-18 and reverted 2026-08-19.** Sub-item (a) — the page reloading itself — DID ship 2026-08-18, and that inline closure glyph is what the marker script misread as a whole-row closure. The rest of the *Known gaps* list in the skill is still open. |
| 51 | 🟡 **Ad-hoc single-video digest — summarize ONE video and dig into it, without owning a playlist — FEASIBILITY STUDY ONLY** — raised by the user 2026-08-18. **The ask:** a user who just wants a summary doc (and dig-deeper from it) for one video should not have to construct a playlist first; give them a temporary holding playlist and the services that hang off it, driven from the CLI. **⚠ SCOPE: investigate and report. Do not build.** **What is already true, measured 2026-08-18:** (a) **there is no user-facing ingest CLI.** Every `package.json` script is ops/maintenance — `rerender-html`, `audit-timestamps`, `audit-summaries`, `repair-timestamps`, `backfill-serial`, `backfill-playlist-titles`, `check:confinement`, `worker`, `verify:gemini-release`, `cloud-sync`, `build:worker`, `check:deployed-bundle`. The local ingest entry point is `runIngestion` (`lib/pipeline.ts:175`), reachable today only through the Next app. (b) **A playlist is not cosmetic — it is the address.** `videos` is keyed `(playlist_id, video_id)` and every artifact lives at `owner/playlist/…`; dig blobs are `dig/<base>/<sectionId>.r<V>.md` **under the playlist principal**. So the user's instinct is right: a holding playlist is the cheapest shape, because everything downstream already assumes one. (c) `recoverOrphanedVideos` (`lib/pipeline.ts:129`) `readdir`-adopts vault files into an index — a possible route in, and the same route backlog #36's Phase 6 flagged as the only entry for hand-placed filenames. **Questions the study must answer, not assume:** where does the holding playlist live (a real `playlists` row? a reserved key? local-vault-only?); does it count against quota/spend like any other; who garbage-collects it and when; does dig-deeper work unchanged once the principal is a real playlist (it should — that is the point of the shape); and does the CLI drive `runIngestion` directly or go through the HTTP API (which settles auth). **Deliverable:** a written feasibility note with a recommendation and the cost of each option — not a spec, not code. | `lib/pipeline.ts`, a new CLI entry, possibly `playlists` | M (study) | (product) | **OPEN — filed 2026-08-18, not started. Feasibility study only.** |
| 52 | 🟡 **Split a focused sub-playlist out of a large one, with the SAME video in several playlists — and decide whether they can share one paid artifact** — raised by the user 2026-08-18. **The ask:** let a user carve a big playlist into smaller coherent ones; a video may appear in several; work out whether existing artifacts can be shared across them, with pros and cons. **⛔ NOT SCHEDULED — DESIGN DIRECTION ONLY. Do not implement. Revisit when priority allows.** **⭐ DESIGN DIRECTION, settled in discussion with the user 2026-08-18. Four decisions, recorded so they are not re-derived — this conversation is the direction, and the alternative was leaving it in a chat log.** **① ACTOR PAYS.** Whoever initiates a cost-incurring action (a summary, a dig) is charged — never the artifact's owner. The alternative creates unbounded liability for anyone who shares, so nobody would. Today actor and owner are always the same principal, so this only clarifies which one wins when they diverge. **The payer is recorded on the GENERATION, not on a path** — a path can be re-pointed, copied into another user's view, or shadowed, and none of that should move a charge. Lands naturally on ADR-0007, which already treats artifacts as an append-only log of generations: payer becomes one more immutable field on the append. **② NON-DESTRUCTIVE SHARE.** The owner may add/update/delete their own artifacts. Everyone else may only *choose-if-multiple*, *add-their-own*, or *hide-from-their-own-view*. Nobody mutates anybody else's work. **This is enforceable as a DATABASE INVARIANT rather than application logic:** migration `0007`'s storage policy is `using (…split_part(name,'/',1) = auth.uid()) with check (…same…)`. **`with check` never changes** — you may only ever write under your own segment — and **only `using` widens** to workspace membership. So "others can add but never clobber" holds regardless of what any route does. **③ A PER-USER VIEW LAYER.** A view maps *(section → which generation I see)*, plus what the user has hidden or added. Rows, not blobs; cheap; and **the one thing that is never shared**. All per-user divergence lives here, which is what keeps the blob layer simple enough for ②'s one-clause change. "User-c sees both digs, or picks one" becomes a row update that never touches storage. **④ IMMUTABLE-IDENTITY ADDRESSING IS THE PREREQUISITE.** Many read paths may resolve to one blob; write uniqueness and read multiplicity stop conflicting once an address derives from immutable identity rather than display attributes. That is **exactly ADR-0006's thesis**, which the user re-derived independently from S3 semantics: *"Blob addresses are derived from immutable identity, not from display attributes."* Two digs of one section are two **generations**, each with its own address. **⚠ THE MEASURED BLOCKER (2026-08-18).** Today the dig key is `dig/<base>/<sectionId>.r<V>.md` — section and doc-version, **no author or generation dimension** — and **there is NO pointer/indirection layer**: `SupabaseBlobStore.objectKey()` feeds straight into `.upload()`, and a grep of `lib/storage/` for a manifest, blob-id or content-hash indirection returns **nothing**. So the path *is* the object, `dig-a` and `dig-b` write the same physical key, and the second **silently overwrites paid work**. The addressing violates the non-destructive policy before any code does — writing "non-destructive" into every route cannot help while the key has no room for two legitimate answers. Note `base = <serial>_<slug>` is derived from two *mutable* values, which is the same defect class as backlog #36: an address built from something that can change. **What the model DISSOLVES, recorded because these were live objections:** *revocation vs paid work* — hiding is a view operation, so no purchase is destroyed by someone else's preference; and *the concurrent double-dig race* — two users digging one section simultaneously is no longer a double charge but two legitimate artifacts, each paid by its own actor. That race was only a defect under the assumption of one dig per section. **⚠ ADR-0008 INTERACTION — needs re-derivation, not a tweak.** The serve money guard is corroboration-by-ordering and is valid only while one grant covers both objects. Widening `using` to a team breaks the *reasoning* quietly: reading the markdown would no longer prove anything about **this** user's access to the model, because they are different subjects. ADR-0008 exists precisely because that guard dies silently. **⚠ STILL GATED on unparking ADR-0006/0007 and the blob-addressing schema (user decision 2026-08-11, task #47) — but the case has changed:** that work was parked partly because its purpose was diffuse (correctness for its own sake). This direction gives it a product reason. **The two halves also separate:** *splitting* a playlist (membership + UI, no money, no parked ADR) can ship without *sharing* artifacts. **Prior art to read before any design work — do not start over.** ADR-0002 explicitly CONSIDERED and REJECTED video-level shared summaries, on **cost** not correctness; ADR-0006 supersedes that rejection in its own words (*"That objection no longer holds once the re-architecture is happening anyway"*) and names the mechanical price: drop `playlist_id` from `jobs_idem_active`, re-point the 1D spend FK, preserve the `(playlist_id, owner_id)` cross-tenant guard. ADR-0006 also already found that a uid-valued path segment grants its creator **unrevocable** access — so "the owner doesn't want to share" is not a separate issue today, it is structurally impossible to withdraw. Backlog #36 burned 13 rounds rediscovering a decision written down in three places; this entry exists to stop that recurring. | `docs/adr/0002`, `docs/adr/0006`, `docs/adr/0007`, `docs/adr/0008`, `jobs_idem_active`, spend FK, migration `0007` storage policy, dig addressing, a new per-user view table | L | (product / addressing) | **OPEN — filed 2026-08-18. NOT SCHEDULED and NOT being implemented; revisit when priority allows.** Design direction ①–④ recorded above from the 2026-08-18 discussion. Blocked on unparking blob addressing (task #47). |
| 53 | 🟢 **A deploy claim has no falsifier for COMMIT drift — only for VERSION drift** — split out 2026-08-18 from the deploy-falsifier discussion, and filed with a **trigger rather than a date** because it should not be built yet. **What IS now covered:** the `/brief` skill reads the running system and compares `flyctl releases` to the roadmap's *"Deployed: release …"* line (PR #111), so *version* drift — prod on v6 while the roadmap claims current — is caught the moment anyone writes a briefing. That is the drift that has actually happened **twice**: production eight days behind on a migration, and 2026-08-18's merged-but-not-deployed money-path fix. **What is NOT covered:** a release built from a stale tree. `flyctl` knows an image digest; **nothing maps it back to a commit**, and a grep of `Dockerfile` + `fly.toml` finds no embedded `GIT_SHA`/`COMMIT`/`BUILD_ID` (measured 2026-08-18). So *"v7 is live"* and *"v7 contains commit X"* are different claims and only the first is checkable. **THE FIX, when it is time:** embed the commit SHA as a Docker build arg, serve it from the deployed app, and let a check derive BOTH sides — no hand-recorded value, exact signal. `scripts/check-deployed-bundle.ts` already establishes the pattern of reading the live app over the network, so it has a natural home. **⛔ EXPLICITLY REJECTED as the cheap alternative:** hand-recording the deploy SHA in the roadmap plus a local *"has master moved?"* check. It depends on a human remembering to write state down — the exact class that produced three stale-state defects on 2026-08-17/18 — and it fires on every docs-only commit, and a gate that cries wolf gets disabled and teaches people to ignore its neighbours. ⛔ **TRIGGER (a) RETIRED 2026-08-21 AS UNFALSIFIABLE — it could never have fired.** It read *\"build it when commit drift actually occurs — a release demonstrably built from a stale tree, which has never yet happened\"*. But **this item's entire subject is that nothing maps a Fly release back to a commit**, so no instrument exists that could observe commit drift happening. The condition waited on evidence it had itself made unobtainable, and the parenthetical *\"has never yet happened\"* was therefore not a measurement — it was the absence of one. This item was not deferred; it was **dormant**. ⭐ Note the recursion, which is the reason this is written out rather than quietly edited: an item about *a claim with no falsifier* shipped with a **build condition** that had no falsifier. Same defect class as the memory `a-checklist-item-can-be-an-unfalsifiable-guard`, one layer up — ask of a trigger, exactly as of a gate, *what observation would make this fire?* **BUILD IT WHEN** either **(a′) someone needs to know which commit a release was built from and cannot answer it** — observable, because it is a *question being asked* rather than a silent event, and it leaves a trace in the transcript — or **(b) the Dockerfile/deploy path is being changed anyway**, making the build-arg nearly free. ⚠ **(a′) HAS ALREADY FIRED ONCE: 2026-08-21**, while assessing this very row — *\"what commit is v7 built from?\"* could not be answered. Recorded as the first tick, **not** as a decision that one occurrence is enough; that threshold is the user's. **(b) checked the same day and FALSE** — `Dockerfile`/`fly.toml` last touched in PR #94, nothing in flight. Until it is built the residual risk is unchanged: a deploy runs from an unexpected tree and nothing says so. | `Dockerfile`, the Fly deploy path, `scripts/check-deployed-bundle.ts` | S | (deploy / gates) | **OPEN — filed 2026-08-18, deliberately NOT scheduled. Version drift is covered by PR #111; this is the commit-drift residue.** ⚠ **AMENDED 2026-08-21: trigger (a) was unfalsifiable and is retired; (a′) replaces it and has one recorded tick.** The item can now actually wake up. Whether one tick is enough to build is the user's call. |
| 54 | 🟠 **Nothing stops the NEXT `public` object from arriving anon-accessible — the platform grants it at CREATE time, and the only defence is that every author remembers** — split out of #30 and #33 on 2026-08-19, after both were re-measured against **prod** (`claude_ro`, read-only) and both came back **true and bounded**. That result is precisely what makes this row necessary rather than redundant. The live exposure is small: 26 of 30 `public` functions are anon-executable, 10 of them `SECURITY DEFINER`, and of those 10 the **8 that could need an ownership check have one** (`auth.uid()`, NULL for `anon`) while the other 2 were read in full and are benign — **no exploitable path found**; `TRUNCATE` is granted to session roles on all five money tables but PostgREST exposes no such verb. **What keeps it small is a convention.** Supabase grants `EXECUTE` to `anon` at `CREATE FUNCTION` time through `pg_default_acl`, and `grant all on all tables in schema public` hands the session roles every table verb; so the safety record above is 8 authors independently remembering, which is a record, not a mechanism. **Revoking on the 26 functions that exist today fixes 26 functions — the 27th arrives exposed exactly as they did**, and #30/#33 keep only that one-time revoke. **Two pieces, and the second is the load-bearing one:** (a) `alter default privileges in schema public revoke execute on functions from anon` and the table-verb equivalent, so new objects do not inherit the grant; (b) **a ratchet** that enumerates `public` functions from `pg_catalog` and **FAILS when a `security definer` function is anon-executable and is not on an explicit allow-list**, plus the money-table TRUNCATE assertion #30 specifies. **(a) without (b) re-commits the defect one level up** — a default privilege is itself something a later `grant` can undo and nobody would see, and this project has already measured that a written convention catches what you happen to read while a script catches what is actually there. ⚠ **Design the allow-list to fail loudly:** enumerate from the catalog and compare against the written list, so an unlisted function is an error — a list that silently stops matching is worse than no check at all. **Where it runs:** the schema gates need a live Postgres and are **not in CI yet** (`docs/dev-process.md` → *"Not yet in CI"*), so this rides the promotion slice that wires them in — the same slice #29 is already parked against — rather than growing a second harness. **Trigger:** whichever comes first — that promotion slice, the next security-hardening pass, or **the next `security definer` function**, which is the real deadline: it will be anon-callable the moment it is created, and today nothing anywhere would say so. | `supabase/migrations/*` (default privileges + the one-time revokes from #30/#33), a new `pg_catalog` assertion alongside `scripts/check-guard-coverage.py`, the promotion slice that puts the schema gates in CI | S (default) + M (ratchet) | (cloud/security) | **HALF DONE — filed 2026-08-19, split out of #30 + #33.** ⭐ **RATCHET HALF (b) SHIPPED 2026-08-21 — `scripts/check-anon-exposure.py`**, 22 self-test cases, mutation-verified (dropping an ALLOW entry turns the named rule red). It enumerates `public` `security definer` functions from `pg_catalog`, FAILS on any that `anon` may EXECUTE and is not allow-listed, and — going beyond what this row asked — **re-checks each allow-list entry's stated reason against the live definition**, so editing the `auth.uid()` guard out of an allowed function fails the gate even though its grant never changed. The #30 TRUNCATE half is a BASELINE (5, may fall, never grow) rather than an absolute assertion, because #30 is still open and a gate that is red from birth gets disabled. **HALF (a) — the `alter default privileges` revoke — REMAINS OPEN**; it changes prod grants and was deliberately excluded. ⚠ **FINDING THAT RESHAPED THE DESIGN: local and prod DISAGREE.** Measured 2026-08-21 — `definer + anon-executable` is **5 locally, 10 on prod**. A ratchet pointed at the docker stack would have reported green over the exposed environment, so the check **defaults to prod** via read-only `claude_ro` and names its subject before its verdict; `--local` opts in. ⚠ **AND IT SHIPPED FAIL-OPEN FOR TEN MINUTES:** `boolean::text` yields `true`/`false` while psql *displays* `t`/`f`, so the first parse read every row as not-exposed and reported **prod clean while ten functions were anon-callable**. `--self-test` was green throughout — the rule/fetch split is **structurally blind to a fetch defect**. Caught only by comparing against a hand measurement taken minutes earlier. The parse is now a pure, tested function that RAISES on anything unexpected. |
| 55 | ✅ (was 🟡) **A video with no AI score is INVISIBLE in the library, and the empty state blames the user's filters** — found 2026-08-20 by backlog #44, the first test ever to render the main pane. `components/cloud/CloudApp.tsx:271` filters with `v.overallScore >= filters.minScore`, and **`undefined >= 0` is `false` in JavaScript**, so an unscored video is dropped even at the `minScore = 0` \"All scores\" default. The pane then renders `VideoList`'s empty branch (`components/VideoList.tsx:75-79`): *\"No videos to show. Try adjusting the filters or enable Show Archive\"* — advice that cannot help, because no filter setting brings the row back. **MEASURED IN PROD 2026-08-21 (`claude_ro`, read-only): 0 live instances.** All 12 videos have both a summary and a score, so nothing is hidden today and this is LATENT, not active — which is why 🟡. **The reachable window, and it is the worst possible moment:** `overallScore` is written only by the summarisation pipeline (`lib/pipeline.ts:119`; :81 defaults it to `3` when Gemini returns nothing), so a video row that exists before its summary completes has no score. That is precisely when `IngestProgressBanner` is telling the user work is in progress — the videos they are waiting for would be absent while the banner says they are coming. ⚠ **UNVERIFIED: whether the cloud ingest creates a `videos` row before summarising.** Prod cannot answer it (every row is already summarised). The observation that settles it: ingest a playlist and watch the library during processing. **Do that before choosing a fix** — if no row exists pre-summary, this is unreachable and the row can be closed as such. **Two candidate fixes, and they are not equivalent:** treat a missing score as passing the filter (`(v.overallScore ?? 0) >= filters.minScore`) — one character of intent, shows the video with a blank score column; or give the pipeline a guaranteed default at write time, which hides the distinction between *not yet scored* and *scored 3*. The first keeps the states distinguishable and is the recommendation. **Guard it:** #44's suite now renders the pane, so a regression test belongs in `tests/e2e/cloud-library.spec.ts` seeding one scored and one unscored video and asserting BOTH are listed. | `components/cloud/CloudApp.tsx:271` | XS (fix) + S (decide + guard) | (cloud/frontend) | ✅ **FIXED 2026-08-21 — the user chose SHOW AS PENDING.** ⭐ **The open question is also ANSWERED, by code rather than by observation: the state is reachable on EVERY ingest.** `claimVideoSlot` INSERTs the row (`0007_storage_and_rpcs.sql:35`, called from `lib/pipeline.ts:235`) and `overallScore` only arrives with `upsertVideo` AFTER the summary is generated — so the window is exactly when `IngestProgressBanner` is promising those videos. No prod observation was needed; the sequence is in the source. ⭐ **AND THE CORRECT BEHAVIOUR WAS ALREADY WRITTEN ONE FILTER BELOW**: `personalScore` has `if (v.personalScore === undefined) return true; // unscored: shown dimmed, not hidden`. `overallScore` was left as a bare comparison. The fix makes the siblings consistent rather than inventing a rule. Also: `VideoRow` now renders `—` instead of an empty cell, and dims on `summaryReady === false` — **`=== false`, not `!== true`**, because the flag is absent for LOCAL videos and `!== true` would dim every row in the local app. **Guarded** by 3 component tests in `tests/components/cloud-app.test.tsx`, mutation-checked: 2 go red without the fix; the third (a scored video below an explicit `minScore` still hides) passes either way BY DESIGN — it exists so this fix cannot quietly turn the AI-score filter into a no-op. The guard is component-level; a browser-level rung seeding one scored + one unscored video remains possible now that #44 renders the pane. |

## Bundles

- **A — Magazine style refinement** *(XS–S)*: #6 + #7 + #5. Prototype the lead + bullet-emphasis variants, user picks, implement. Stacks on PR #3. ← **STARTED** (branch `feat/magazine-style-refinement`).
- **B — Deep-dive parity** *(M–L)*: #1 + #3 (folded in, since #8 is someday). Brings deep-dive to the summary's version-aware/hourglass/timestamp parity.
- **C — Output formats** *(M + decision)*: #2. Blocked on the PDF-vs-printable-HTML product call.
- **D — Aligned detail view** *(XL, someday)*: #8. Own staged roadmap when revisited.
- *(Loose: #4 — fold into A or B opportunistically, or skip.)*

## Found during testing (2026-06-19/20)

| # | Item | Status |
|---|------|--------|
| 9 | ✅ (was 🟡) **CorrectionsPanel hydration bug** — `<div>` overlay rendered inside `<tbody>` (pre-existing since the corrections feature, `10a21ee`). Fix: portal the overlay to `document.body`. | ✅ **DONE** (`611a7bc`): `createPortal(<>…</>, document.body)` in `CorrectionsPanel.tsx` + regression test (renders inside a `<tbody>`, asserts the backdrop portals out to `<body>`). |
| 10 | ✅ (was 🟡) **Gemini retry-with-backoff** — was a hardening idea; became the real fix for the intermittent re-summarize failures. | ✅ **DONE** (`24a56c7`): `generateJson` helper retries malformed-JSON / transient API errors on `generateSummary`/`generateMagazineModel`/`extractQuickView`. Root cause was Gemini intermittently emitting invalid JSON despite JSON mode; unguarded `JSON.parse` killed the whole op. Verified 3/3 on #80. |
| 11 | ✅ (was 🟡) **Dev error logger** — record full error chain to a checkable place. | ✅ **DONE** (`32a90b5`): `lib/dev-logger.ts` → `logs/dev-errors.log` (full `caused by:` chain + stack) wired into html-doc/deep-dive/regenerate/ingest routes; `errorSummary` surfaces the full chain to the UI. |

Possible minor follow-up: strip markdown code fences / handle leading `[` before `JSON.parse` (seen once on the magazine call) — retry already covers it, so low priority.

## Sequence

1. **A** (now) → 2. **B** → 3. **C** (after PDF decision) → 4. **D** (someday). Interleaved: **#9** (corrections portal) is a small independent bug fix; **#10/#11** already shipped on `chore/dev-error-logger`.

**Decisions recorded:** #8 = someday → #3 does NOT wait for #8; it rides Bundle B. #1 is built standalone (it's orthogonal to #8's alignment, so no rework).

## Notes
- All near-term bundles stack on PR #3 (`feat/summary-deepdive-quality`) → merge order: #1 timestamps → #2 versioned-regen → #3 quality-pass → A → B …
